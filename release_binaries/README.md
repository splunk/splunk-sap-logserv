# v0.0.6.0 Release Binaries

This directory contains the three installable tarballs for the **Splunk for SAP LogServ** v0.0.6.0 release (the Search-Head App, the Data TA, and the per-Heavy-Forwarder Azure ingest add-on).

## Canonical tarballs

| Tarball | md5 | Size | Tier |
|---|---|---|---|
| [`splunk_app_sap_logserv-0.0.6.0.tar.gz`](./splunk_app_sap_logserv-0.0.6.0.tar.gz) | `544106bb503d09ab14a9e30067e3c204` | 3.87 MB | Search Head |
| [`splunk_ta_sap_logserv-0.0.6.tar.gz`](./splunk_ta_sap_logserv-0.0.6.tar.gz) | `952df3ebfe8b1ed1b06b0dd642409b1b` | 1.56 MB | Deployment Server + Heavy Forwarders + Indexer (also installable on Splunk Cloud SH for single-instance Cloud deployments) |
| [`splunk_ta_sap_logserv_azure-0.0.6.tar.gz`](./splunk_ta_sap_logserv_azure-0.0.6.tar.gz) | `6eae44d0df06489127fa9815df55485f` | 1.54 MB | Heavy Forwarders only — Azure ingest add-on; **install per-HF, NOT via the Deployment Server** (only needed for Azure Blob ingest) |

The App tarball is at **build 252** (internal `app.conf` / `app.manifest` version `0.0.6`, proper 3-part SemVer). **Build 252 bumps `react-router-dom` 7.14.2 → 7.18.1** (security hygiene) so the dependency clears [GHSA-4hjh-wcwx-xvwj](https://github.com/remix-run/react-router/security) (a `__manifest` DoS affecting React Router *Framework Mode* / Remix, patched in 7.15.0) — see "Build 252" below. **Build 251 makes the 4 whole-estate AI Assistant prompts (top hosts / noisy hosts / distinct hosts / volume-drop) read from a KV-Store rollup** instead of a raw `\| top host` full-scan, so they complete in sub-second time at any window instead of timing out at the 30 s MCP client limit on a large dataset — see "Build 251" below. **Build 250 fixes the AI Assistant canned-prompt time-range dropdown** so it actually bounds the dispatched saved search — previously every predefined prompt ran unbounded over the whole index via the Splunk MCP Server and, on a large dataset, timed out client-side ("Error: signal is aborted without reason"); see "Build 250" below. **Build 249 enables the Enterprise Security content by default** on a re-staggered, collision-free schedule and reworks the 13 ES AI Assistant Security-pack prompts so they function as interactive prompts (the FATAL `anomaly_topology_edge_volume` fix, inline `_time`-based off-hours logic replacing the dead-constant `is_business_hours`/`is_weekend` fields, the `service_account_interactive` field fix, and the heavy 30-day anomaly scans moved to a daily cadence) — see "Build 249" below. **Build 248 fixes + speeds up AI Assistant predefined prompts** found in a v0.0.6 audit: two proxy prompts (`logserv_proxy_top_denied_urls`, `logserv_firewall_proxy_correlation`) that filtered the no-longer-emitted Squid value `action=blocked` now use `action="denied"` (they were returning empty), and four estate-wide "overview" prompts (`total_events_kpi`, `events_by_clz_dir`, `event_volume_by_sourcetype_trend`, `data_freshness`) were converted from raw full-scans to `tstats` (sub-second vs. 50–95 s at scale); byte-exact-verified, AppInspect-clean. See "Build 248" below. **Build 247 gives the two "Sourcetype Mapping" link-graph panels** (Data Pipeline Overview + Host Details) the standard orange-dot loading spinner instead of a plain "Loading link graph…" text; JS-only, see "Build 247" below. **Build 246 sorts the Dashboard Data tab's per-rollup table alphabetically by dashboard name** (was registry-definition order); JS-only, see "Build 246" below. **Build 245 consolidates the Settings page** — the former "Topology" and "Dashboard Data" admin tabs are merged into one **Dashboard Data** tab that manages every KV-Store rollup uniformly (topology graph + beaconing folded in), and the whole page is reorganized into a two-level **AI Assistant · Dashboard Data** tab hierarchy renamed **Application Settings** (route `#/settings`); see "Build 245" below. UI-only — no conf/data change. **Builds 243–244 move the last two raw-scan panel groups — Host Details "Role Activity" + the source→sourcetype "Sourcetype Mapping" graphs — onto KV-Store rollups** so they stay fast at high event volume (build 244 adds the two new rollups to the Settings → Dashboard Data backfill panel) — see "Build 243–244" below. **Build 242 ships the Enterprise Security content disabled by default and re-staggers the scheduled searches** to eliminate the hourly burst and all same-minute collisions — see "Build 242" below. Build 240 caches the Environment Topology view's right-pane detail tabs onto an hourly KV-Store rollup (the last topology corner still doing live raw scans per node/edge click); **build 241 host-splits the three full-index node aggregate arms so the one-time backfill no longer does a full-index `mvexpand`** — see "Build 240–241" below. Build 239 added the donut/pie empty-after-range-change fix.

The **Data TA is at internal `app.conf` version `0.0.6`** (md5 `952df3ebfe8b1ed1b06b0dd642409b1b`). It is the single shared index-time pipeline for **all** LogServ data, regardless of ingest channel: `clz_dir`/`clz_subdir` sourcetype routing, the Configuration → Filters `nullQueue` filtering, the `_time` drop, and the `splunk_solution`/`cloud_provider` stamping — installed on the Deployment Server + every Heavy Forwarder + Indexer. It carries **no ingest input of its own**; data collection is the forwarder-tier add-ons' job — `Splunk_TA_aws` for AWS S3, and the new **`splunk_ta_sap_logserv_azure`** for Azure (below) — each emitting `sourcetype = sap_logserv_logs` into this pipeline. All other Data TA behavior (filter routing, Cloud Provider attribution, the filesystem-fallback Cloud-Victoria fix from the prior "fsfix" build) is carried forward unchanged. The entire v0.0.6.0 **dashboard-performance** effort remains search-head-side (dashboards + KV-Store rollups + scheduled aggregation searches, all in the App); the Data TA does no parsing change in v0.0.6.

The **Azure ingest add-on — [`splunk_ta_sap_logserv_azure-0.0.6.tar.gz`](./splunk_ta_sap_logserv_azure-0.0.6.tar.gz)** (md5 `6eae44d0df06489127fa9815df55485f`) — is a new, standalone, **Heavy-Forwarder-tier** TA that provides **queue-driven Azure Blob ingest**, the Azure twin of `Splunk_TA_aws`'s SQS-Based-S3. Its **`sap_logserv_azure_queue`** modular input consumes Azure **Event Grid → Storage Queue** `BlobCreated` notifications, fetches each LogServ blob over a SAS, and emits its NDJSON via the native modular-input EventWriter — so events flow through the Heavy Forwarder's index-time pipeline and reuse the Data TA's routing/filtering/stamping unchanged (it emits a **fixed** `sourcetype = sap_logserv_logs` — hard-coded in the input, with the **Sourcetype** field shown read-only in the form, since the Data TA's pipeline keys on that sourcetype — and stamps `_meta = cloud_provider::azure`). The input is **stdlib-only** (no Azure SDK) and inert until an input instance is configured. **Install it per Heavy Forwarder — NOT via the Deployment Server:** the SAS lives in the TA's own `local/passwords.conf`, so it must not be DS-managed (a DS push would overwrite that `local/`). This mirrors how `Splunk_TA_aws` is configured per-HF and makes Azure ingest symmetric with AWS. (Earlier v0.0.6 iterations shipped this input bundled **inside** the Data TA via the fragile "Option B" `system/local` SAS pattern; it was split into this dedicated add-on so the per-host secret is no longer inside a DS-managed app.) The prior Azure TA builds (`cc0f9a6319582ddc07dca5e3079c900e`, before the Sourcetype was fixed; `22ac6035688b55242f58f9503d0802eb`, an interim build that made the Sourcetype a single-option `singleSelect` — which failed to render in the UCC Inputs table, so the field reverted to a read-only `text` field), the prior with-Azure Data TA (md5 `c69a91f5568684af97eee52dac8c8d32`), and the pre-Azure "fsfix" Data TA (app version `0.0.5`, md5 `dcd9ba2a966ea5acc6c726db364cda77`) are preserved under [`../testing/iteration_tarballs/`](../testing/iteration_tarballs/). See [Azure Setup](../docs/content/install-setup/azure-queue-setup.md) for the per-HF install + input configuration.

All three are installable via Splunk Web (**Apps → Install app from file**) or via CLI:
```bash
/opt/splunk/bin/splunk install app /path/to/<tarball>
```
(Install the App on the Search Head, the Data TA on the Deployment Server + Heavy Forwarders + Indexer, and — only for Azure ingest — the Azure add-on **directly on each Heavy Forwarder, not as a deployment-app**.)

See [`docs/content/getting-started/quick-install-reference.md`](../docs/content/getting-started/quick-install-reference.md) for the per-tier install matrix and the prerequisite Splunkbase add-ons (CIM modules, Splunk MCP Server for the AI Assistant, the AWS / Microsoft Azure ingest add-ons).

## What's new in v0.0.6.0 — dashboard performance refactor

v0.0.6.0 keeps the **entire v0.0.5.0 feature set unchanged** (21 React dashboards + the Environment Topology view, templates-only AI Assistant with 61 canned prompts + audit log, index-time filtering + Deployment Server automation, Cloud Provider attribution, Enterprise Security integration). The single focus of this release is a **dashboard data-layer rewrite** that makes the dashboards fast at high event volume.

**The problem.** At a customer's reported scale (~10.7M events / 24 h, ~321M over 30 days), the 21 non-Topology dashboards took **>10 minutes** to populate. Each panel dispatched its own raw, search-time-extraction full-scan with no shared base search, no `tstats`, and no acceleration. (The Topology view was already fast — it had been moved to a KV-Store-backed data layer in an earlier release.)

**The fix (App builds 198–237).** Every dashboard panel was re-tiered onto the cheapest correct data source, byte-exact-verified against the original raw SPL. The **final architecture (build 237) has two data tiers** — `tstats` on indexed dimensions, and KV-Store precompute rollups:

1. **`tstats` on indexed dimensions** (builds 198–200) — Data Pipeline Overview, Host Details, and the Environment Health pipeline-count panels read pure counts/avgs via `| tstats` over the already-`WRITE_META`-indexed fields. A new `default/fields.conf` declares `INDEXED = true` for `clz_dir` / `clz_subdir` / `splunk_solution` / `cloud_provider` so `tstats` can read them at search time.
2. **KV-Store precompute rollups** (builds 206–232) — **every** other high-volume dashboard reads from hourly-aggregated KV Store rollup collections. Scheduled saved searches (`logserv_*_aggregate`, cron `5 * * * *`) precompute each panel's data; the dashboards read it back with a `bucket_ts` range filter driven by the global time-range picker. Streamstats/event-listing panels that cannot be rolled up stay raw, capped with `| head N` where they are time-ordered listings.

   - Builds 206–227 covered Work Process Performance, ABAP Operations, Environment Health error classification, HANA Audit, Change & Configuration Activity, SAP Router, ABAP Network & Security, Cross-Stack Authentication, Network Perimeter, Linux, Web & API Performance, HANA Trace, Windows, SAP Services, and Multi-Cloud Overview.
   - **Build 232 retired the interim CIM tier and folded the percentile panels into the rollups** (see below) — bringing four more dashboards (Proxy, Web Dispatcher, Cloud Connector, DNS Analytics) fully onto KV-Store as well.
   - **Build 233 moved the Host Details Overview tab's last raw full-scans onto the rollup tier** — its DATA VOLUME (`sum(len(_raw))`), ERRORS/CRITICALS, and AUTH FAILURES KPIs + sparklines were ~162 s / 29 s / 38 s at 76 M events (minutes at full volume); they now read a new `logserv_hostdetails_rollup` (metrics `vol`/`err`/`auth` per host+bucket) in ~0.1–0.3 s (≈200–580× faster). The Event Count by Sourcetype chart was also rewritten to `tstats` (165 s → 5 s, 30×; sourcetype is indexed). The release now ships **20 rollup collections** (15 from builds 206–227, plus `logserv_cloudconn_rollup`, `logserv_proxy_rollup`, `logserv_dns_rollup`, `logserv_pipeline_rollup`, `logserv_hostdetails_rollup`) plus a per-day beaconing rollup.

**Build 232 — CIM tier removed + percentiles → Avg + Max.** An interim approach (builds 201–205) had the four HTTP/DNS/proxy-semantic dashboards read their status / method / bandwidth / domain panels from the CIM **Web** / **Network_Resolution** / **Authentication** accelerated data models, shipping `summariesonly=false allow_old_summaries=true`. The problem: when a customer had **not** accelerated those models (the common case), the queries fell back to a raw full-scan — exactly the slowness this release set out to fix. Build 232 therefore **ditches the CIM tier entirely** and moves those four dashboards onto dedicated KV-Store rollups, so they are fast regardless of the customer's CIM-acceleration state. In the same build, **every percentile chart (p50/p95/p99) was replaced with an Avg + Max-by-hour chart** — averages and maxima roll up byte-exact across hourly buckets (Avg = Σsum ÷ Σcount, Max = max-of-per-bucket-max), whereas percentiles cannot be merged across buckets.

**Result (validated at 335M events on the test fleet).** Cached reads are uniformly **~0.1–0.6 s** versus 10 s – 19 min raw; speedups range **64× – 5,358×**. Every rolled-up panel was verified byte-exact against its original raw SPL (build 232: all 40 refactored panels re-verified byte-exact post-deploy at 335M; build 233: the 7 Host Details panels re-verified byte-exact). Data freshness on the rolled-up panels is hourly (the same trade-off the Topology view already made).

**Builds 234–237 — universal panel UX + the last raw-scan fixes.**

- **Loading spinner on every panel** (build 234) — every chart/table renders the orange-dot spinner + "Loading data…" while its search is in flight (KPI cards show a small spinner instead of a dash), replacing the old plain "Loading…" text.
- **Per-panel action toolbar** (builds 234–235) — every chart and table panel header now carries **Open in Search · Download (CSV) · Inspect (job inspector) · Refresh** plus a "&lt;1m ago" last-run stamp. Charts auto-wire their search metadata; tables pass it explicitly. (KPI single-value cards get the spinner but no toolbar.)
- **Web Dispatcher "Slowest Request Traces"** (build 236) — was the last estate-wide raw `| sort 20 - total_us` full-scan of the highest-volume web sourcetype (~165 s at scale). Now reads a per-hour top-20 rollup (`logserv_webdisp_slowtrace_rollup`); **byte-exact** (the global 20 slowest are each within their own hour's top-20, so the union re-sorted = the global top-20).
- **Beaconing / suspicious-activity** (build 237) — the 3 DNS / Network-Perimeter `streamstats` periodicity panels now read a per-day gap-statistics rollup (`logserv_beaconing_detail_rollup`), reconstructing avg/variance over the picked range. This is a **close approximation** (per-day gaps miss the inter-arrival gap spanning each midnight boundary) chosen so the panels stay time-picker-responsive; on the validation data the flagged set matched the raw streamstats exactly.

The release now ships **22 rollup collections** (the 20 from builds 206–233 plus `logserv_webdisp_slowtrace_rollup` and `logserv_beaconing_detail_rollup`) plus the per-day beaconing-count rollup.

**User-visible changes in build 232.** A few panels changed shape as a consequence of moving to rollups: HANA Trace "Slowest SQL Operations" became a top-operations-by-Max/Avg-duration table (the per-event `_time`/host columns cannot survive an aggregate); Web Dispatcher "Top URIs" dropped its "Unique Clients" column (a 3-dimension grain would explode at scale); and all response-time charts now show **Avg + Max** instead of p50/p95/p99. Network Perimeter's "Suspicious Activity Indicator", DNS beaconing tables, Web Dispatcher "Slowest Request Traces", and Cloud Connector "Audit Log" stay raw (streamstats / per-event listings that cannot be rolled up byte-exact).

### Build 252 — react-router-dom 7.14.2 → 7.18.1 (security hygiene)

Bumps the `react-router-dom` dependency from `^7.14.2` (which locked react-router **7.14.2**) to `^7.15.0`, resolving to **7.18.1** — the latest 7.x, well past the patched **7.15.0**. This clears [GHSA-4hjh-wcwx-xvwj](https://github.com/remix-run/react-router/security): a DoS via unbounded path expansion on the `__manifest` endpoint in react-router 7.0.0–7.14.x / `@remix-run/server-runtime` 2.10.0–2.17.4.

**The LogServ App was never exploitable** — it's a client-side webpack SPA served by Splunk Web, routed entirely by `<HashRouter>` (Declarative Mode, which the advisory explicitly exempts), with no React Router/Remix server runtime and therefore no `__manifest` endpoint. The bump is **hygiene only**: it stops SCA / dependency scanners from flagging react-router 7.14.2 in the SBOM. No app behavior change — `tsc` passed clean, and routing was smoke-tested live (index / settings / dashboard / topology routes + the NotFound fallback, zero console errors). The SBOM + `THIRD-PARTY-NOTICES.md` now report react-router / react-router-dom **7.18.1**. AppInspect Cloud unchanged at 0 / 0 / 0 / 8 warnings / 109 success.

### Build 251 — whole-estate AI Assistant host prompts moved onto a KV-Store rollup

Build 250 made the prompt time-range dropdown bound the dispatched search, but four **whole-estate** predefined prompts were still raw `\| top host` / `dc(host)` / per-host-timechart full-scans over the entire index. On a large dataset (hundreds of millions of events) those exceed the MCP client's 30 s request timeout at wide windows — e.g. *Top hosts by event volume* at **−30 days** took **> 150 s** and aborted with *"Error: signal is aborted without reason."* Build 251 reads them from the existing `logserv_hostdetails_rollup` KV-Store rollup instead:

- **Four prompts rewritten** — `logserv_top_hosts_by_volume`, `logserv_noisy_hosts_trend`, `logserv_distinct_hosts_kpi`, `logserv_event_volume_drop` now read the hourly `(host, bucket)` rollup (`metric="vol"`) via `\| inputlookup … \| addinfo \| where bucket_ts in window`, so each returns in **sub-second** time at any window. Measured on a 335 M-event instance: top-hosts at −30 d went from **> 150 s (abort) → 0.6 s**; −24 h = 0.5 s; the dropdown window still drives the result (the −30 d and −24 h answers differ).
- **Byte-exact, not approximate.** The rollup's `vol` metric is built from a raw `\| stats count by host` (so it uses the same search-time `host` field — including the `sap:hana:audit` `FIELDALIAS-host = audit_hostname AS host` rewrite — that `\| top host` uses). A `tstats` swap was rejected precisely because it would read the *indexed* host and silently change the displayed hosts. Each rewritten read was verified field-for-field against the original raw SPL (including `top`'s percent precision and the timechart's null-host `NULL` series, reproduced via `eval host=if(host="(none)",null(),host)` + `fillnull value=0`).
- **No new infrastructure.** It reuses the rollup that already powers the Host Details dashboard (already backfilled, hourly-scheduled, and in the Settings → Dashboard Data backfill panel). On a fresh install the prompts populate older windows once the customer runs the Dashboard Data backfill, same as every dashboard. The intent map (`spl`) and `savedsearches.conf` (`search`) were updated in lockstep (byte-equal, per the build-time consistency check). AppInspect Cloud unchanged at 0 / 0 / 0 / 8 warnings / 109 success.

### Build 250 — AI Assistant canned-prompt time-range dropdown now bounds the search

The AI Assistant's "Browse predefined prompts" window has a **Time range** dropdown (Last 1h … Last 30d). Build 250 makes it actually drive the dispatched saved search. Previously the selected window was silently dropped, so every predefined prompt ran **unbounded over the entire index** through the Splunk MCP Server (App 7931 `run_saved_search`), which — on a large dataset — took minutes and timed out client-side with *"Error: signal is aborted without reason."*

- **Root cause (webapp side).** `MCPClient.runSavedSearch()` nested the caller's `earliest_time`/`latest_time` under an `arguments` object in the MCP tool call. App 7931's `run_saved_search` tool declares `earliest_time`/`latest_time` as **top-level** tool arguments (its token-substitution field is the string `args`; there is no `arguments` property), and its validator silently ignores unknown keys — so the time range never reached the dispatched search. The fix **flattens** the time args to the top level of the tool call; the `splunk_run_query` path already did this correctly and is unchanged.
- **Both prompt paths fixed.** The canned-prompt path (`runCannedPrompt`) and the AI-driven `splunk_run_saved_search` path both funnel through `runSavedSearch`, so the one-method fix covers both.
- **Verified end-to-end** through the webapp's exact MCP auth + transport on a 335M-event instance: the same saved search dispatched with a `-24h` window completes in ~1 s, while the old nested shape ran unbounded (>3 min, the source of the abort); and the same prompt at `-60m` vs `-7d` returns different bounded row counts (2 vs 169 hourly bins) — the dropdown now parameterizes the search. AppInspect Cloud unchanged at 0 / 0 / 0 / 8 warnings / 109 success (a webapp/JS + `app.conf`-build change only). The saved searches themselves are unchanged — no fixed window was baked in.

### Build 249 — Enterprise Security enabled by default + ES prompt rework

Building on build 248's prompt fixes, build 249 turns the Enterprise Security content **on by default** and makes the 13 ES Security-pack AI Assistant prompts usable:

- **All 22 `splunk_sap_logserv_es_*` searches now ship enabled** (`disabled = 0`), reversing build 242's disabled-by-default decision. They run on a re-staggered, **collision-free** schedule (verified: 0 collisions among all 76 enabled scheduled searches): 16 correlation searches hourly at the disjoint minutes `:29` / odd `:31`–`:59`, the 2 Asset/Identity feeds every 4 h at `:00` / `:01`, and the 4 behavioral-anomaly searches once **daily** at `:02`. To fit the collision-free schedule, eight correlation searches that ran every 5–15 min now run hourly (matched 65-min dispatch windows), and the four anomaly searches run daily instead of hourly — a daily run still evaluates every hourly bucket of the prior day, so no detections are missed, and the two heavy 30-day scans no longer run every hour. Dual-mode is unchanged: `action.notable`/`action.risk` no-op without ES installed. To disable or re-tune, see [Enterprise Security → Disabling or tuning the ES content](../docs/content/enterprise-security/overview.md).
- **The 13 ES prompts were reworked to function as interactive prompts.** Dispatched by name, they had errored on click ever since build 242 disabled their searches; enabling fixes that, and the SPL was repaired — the `anomaly_topology_edge_volume` FATAL `inputlookup … where` error; the dead-constant `is_business_hours`/`is_weekend` off-hours fields (the props `audit_datetime`→`hour_of_day` EVAL-chain doesn't resolve) replaced with inline `_time`-based off-hours logic; `service_account_interactive` fixed to detect service accounts via `executing_user` (the old `target_user`-based field was empty on CONNECT events); and the structurally-empty `after_hours_admin_data_access` reworked to off-hours HANA-admin activity by user+host with same-host off-hours web-traffic annotation. Intent map → v0.0.11. All 13 live-verified by name on build 249 (0 dispatch failures, 0 search failures); AppInspect Cloud unchanged at 0 / 0 / 0 / 8 warnings / 109 success.

### Build 248 — AI Assistant predefined-prompt fixes (audit follow-up)

A v0.0.6 audit of all 61 AI Assistant predefined prompts (run live against 388M events) found two correctness bugs and an efficiency gap; build 248 ships the safe, byte-exact-verified fixes:

- **Two proxy prompts un-broken.** `logserv_proxy_top_denied_urls` and `logserv_firewall_proxy_correlation` filtered `action=blocked`, but in v0.0.6 the absorbed `squid_actions_210.csv` lookup emits `action ∈ {NONE, allowed, denied, failed}` — never `blocked` — so the first returned **nothing** and the second's proxy half was dead. Both now use `action="denied"` (matching `logserv_top_error_categories`). Live-verified: `proxy_top_denied_urls` now returns the denied destinations; the correlation now includes the proxy layer.
- **Four "overview" prompts converted to `tstats`.** `logserv_total_events_kpi`, `logserv_events_by_clz_dir`, `logserv_event_volume_by_sourcetype_trend`, and `logserv_data_freshness` were raw full-scans of the entire index (54–95 s at one-day scale, minutes over wider ranges). They now read via `| tstats` (the macro stays in the `WHERE` clause so per-customer index overrides still work), returning **identical results in ~0.5–1 s** (verified byte-exact on the test fleet).
- **Held intentionally (not converted):** `top_hosts_by_volume`, `noisy_hosts_trend`, `distinct_hosts_kpi`, `event_volume_drop` — these group by `host`, and `tstats` reads the *indexed* host while these prompts use the *search-time* host (some events carry an FQDN alias), so a `tstats` swap would silently change the displayed host counts. They remain raw (correct); making them fast needs a rollup keyed on the search-time host, a separate change.
- **Not addressed here (separate decision):** the 13 `splunk_sap_logserv_es_*` prompts in the Security pack error on click because build 242 set their saved searches `disabled = 1` (a disabled search can't be dispatched). The audit recommends removing them from the prompt registry (they are ES scheduled detections, not interactive prompts) or re-enabling them unscheduled — deferred to a product decision.

Both the intent map (`logserv_intent_map.json`) and `savedsearches.conf` were edited byte-equal (consistency check clean across all 61). AppInspect re-run — **unchanged at 0 errors / 0 failures / 0 future_failures / 8 warnings / 109 success.**

### Build 247 — Sourcetype Mapping loading spinner

The two **Sourcetype Mapping** link-graph panels (the Data Pipeline Overview "Sourcetype Mapping" tab and the Host Details "Sourcetype Mapping" tab) rendered a plain centered "Loading link graph…" text while their search was in flight, unlike every other panel, which shows the shared orange-dot spinner + "Loading data…" (`PanelLoading`, build 234). Both now use `PanelLoading` for a consistent in-flight visual. JavaScript + `app.conf`-build change only — AppInspect re-run, unchanged at 0 errors / 0 failures / 0 future_failures / 8 warnings / 109 success.

### Build 246 — Dashboard Data table sorted alphabetically

A one-line follow-on to build 245: the **per-rollup table** on the Dashboard Data settings tab now renders **alphabetically by dashboard name** (ascending) instead of the registry-definition order, so an admin can scan it by name. The registry array itself is unchanged (everything else iterates it order-independently — the master switch, completeness check, and "Run backfill (all)"); only the table render sorts a copy. JavaScript + `app.conf`-build change only — AppInspect re-run, unchanged at 0 errors / 0 failures / 0 future_failures / 8 warnings / 109 success.

### Build 245 — Settings consolidation + two-level tab hierarchy

A **UI-only** refactor of the admin Settings page (no conf, collection, saved-search, or data change — every rollup + search already exists). Two coupled changes shipped in one build:

- **Merged the "Topology" + "Dashboard Data" Settings tabs into one "Dashboard Data" tab.** The former Topology tab managed only the Environment Topology graph (nodes/edges) with bespoke enable/cron/retention/clear controls; the Dashboard Data tab backfilled the other rollups. They're now one tab that manages the **entire rollup data layer uniformly**: a global **hourly-aggregation master switch** (Enabled / Disabled / Mixed across all rollups), a single **365-day retention** display, the one-click **30-day backfill**, a **per-rollup table** (Dashboard · Schedule · per-rollup aggregation toggle · History · Backfill / Clear actions), and a **Danger Zone** global "Clear all rollups". The Environment Topology **graph** is now just one more row — and **two previously-orphaned rollups were folded into the registry**: the topology graph (`logserv_topology_nodes` / `_edges` / `_inventory`, which the unified backfill never covered) and the build-237 **beaconing-detail** rollup (`logserv_beaconing_detail_rollup`, which had aggregate + backfill searches but no UI). The panel now lists **25 logical rollups across 28 KV-Store collections** (reconciled exhaustively against `default/savedsearches.conf` + `collections.conf`). Folding the topology graph in also switched its backfill from the old single-`| union` saved-search dispatch (which **truncated at scale**) to the per-arm **top-level dispatch**, so the topology backfill no longer truncates at high event volume.
- **Two-level tab hierarchy + page rename + route rename.** The page is now **Application Settings** (`#/settings`, with a back-compat redirect from the old `#/settings/ai-assistant`). Top level: **AI Assistant · Dashboard Data**. The AI Assistant tab holds the four existing panels as a lighter, secondary sub-tab row: **General · Provider Credentials · Splunk MCP · Audit Log**. The default landing is unchanged (AI Assistant ▸ General). Doc paths change from "Settings → AI Assistant → Dashboard Data → Run backfill" to **"Settings → Dashboard Data → Run backfill"** (Dashboard Data is now a top-level peer).

The four AI Assistant panels and the rollup-backfill orchestration were **moved, not rewritten**; the dispatch/poll/completeness REST infrastructure is the proven build-230 mechanism. Live-verified on the test fleet at 335M events: both top tabs, the secondary sub-tabs, the merged table (25 rollups / 28 collections, master switch Enabled-all, per-rollup toggle round-trips through REST, the topology + beaconing rows present + complete), and the `#/settings/ai-assistant` → `#/settings` redirect all work. JavaScript + `app.conf`-build change only — AppInspect re-run, **unchanged at 0 errors / 0 failures / 0 future_failures / 8 warnings / 109 success**.

### Build 243–244 — Role Activity + Sourcetype Mapping → KV-Store rollups

Two panel groups on Host Details / Data Pipeline Overview were the last raw-ish scans left after the v0.0.6 refactor. Fine at low volume, slow once the data is dense (measured at ~10.7M events/day):

- **Sourcetype Mapping** (Host Details Tab 3 + Data Pipeline Overview "Linked Graph") — both already used `tstats`, but `| tstats … BY sourcetype, source, host` was **13.8s @ -7d / 49.7s @ -30d** and rendered **1,225–4,151 LinkGraph nodes**. The driver is `source`: the data date/UUID-stamps it (e.g. SCC audit `…/<UUID>/audit-log_<UUID>_<DATE>.csv`), so its cardinality balloons with the window.
- **Role Activity** (Host Details Tab 2) — 7 raw search-time `top limit=0 <field>` scans, each scanning its sourcetype over the window (≈6.4s @ -30d for the busiest, and slower on real data where every panel is populated).

Both now read hourly KV-Store rollups (the same model as the rest of v0.0.6):

- **`logserv_stmap_rollup`** — `(sourcetype, source, host)` counts per hour, with **`source` normalized** in the aggregate (UUIDs, ISO dates, and 4+digit runs stripped to collapse per-day/per-tunnel variants into logical sources). This makes the read fast **and** the graph far cleaner: the tuple count drops from 4,151 → **318** over -30d, and no longer grows with the window. **Result: 49.7s → 2.1s (~24×)**, plus a much lighter browser render.
- **`logserv_hostrole_rollup`** — a metric-discriminator rollup (`hana_action` / `abap_wp` / `webdisp_status` / `saprouter_peer` / `win_eventcode` / `sudo_user` / `dns_query`) of per-`(host, value)` counts; the panel read reproduces `top`'s value + count + percent **byte-exact** (verified against the raw distribution via the REST search path the app uses). **Result: 6.4s → 0.77s (~8×).**

**User-visible change:** the Sourcetype Mapping graph now shows **normalized logical sources** (e.g. one `…/audit-log_<id>_<date>.csv` node instead of one per tunnel-UUID per day) rather than the exact dated/UUID'd source strings — the date/UUID is noise for a source→sourcetype mapping, and the old graph rendered thousands of nodes at scale. Role Activity is unchanged in appearance (same value / count / percent breakdown).

Adds 2 KV-Store collections + 6 saved searches (aggregate `:03`/`:04`, backfill disabled-by-default, retention `01:52`/`01:54` — all collision-free in the build-242 staggered schedule). **Build 244** adds the two new rollups to the **Settings → Dashboard Data** "Run backfill" panel so a fresh install can seed them one-click (26 rollups listed). AppInspect re-run — posture unchanged at 0/0/0 + 8 warnings / 109 success.

### Build 242 — Enterprise Security disabled by default + scheduled-search staggering

This is a **`savedsearches.conf`-only** release (plus the build-number bump) — no dashboard, rollup, or JavaScript changes. It tunes the scheduled-search workload so it scales cleanly at high event volume.

- **Enterprise Security content ships disabled by default.** All 22 `splunk_sap_logserv_es_*` saved searches (5 correlation searches, the threat-intel + behavioral-anomaly detections, the risk-notable threshold, and the Asset & Identity feeds) now carry `disabled = 1`. The ES content is opt-in — it targets the ES notable / risk / Asset & Identity frameworks and CIM data models that no-op when ES is not installed — and **no dashboard reads any ES output** (verified: the ES searches write only `action.notable` / `action.risk` and the `splunk_for_sap_logserv_{assets,identities}.csv` lookups). Disabling them removes the two heaviest searches on the box (the 30-day anomaly scans) and the per-hour correlation cluster from every customer that has not installed ES. **Enable them after installing Splunk Enterprise Security** — Settings → Searches, Reports & Alerts → filter `splunk_sap_logserv_es_` → Enable, or a `local/savedsearches.conf` `[<name>] disabled = 0` override. See the ES Integration docs.
- **The hourly rollup aggregates were de-bursted.** Previously all 24 always-on rollup-aggregate searches fired at minute `:05` every hour (alongside the two heaviest ES anomaly scans). They are now spread one-per-minute across **`:05`–`:28`** (`<min> * * * *`; the dispatch window is unchanged `-1h@h..@h`, so the firing minute is freshness-neutral — each always processes the just-completed hour). Peak concurrency drops from 24 to ~1–2.
- **Retention + the two daily beaconing aggregates moved to an off-peak `:30`–`:58` band**, 2 minutes apart, across hours 0 and 1 (`00:30`–`00:58` then `01:30`–`01:50`). This keeps every daily search out of the aggregate band and eliminates the two pre-existing same-minute collisions.
- **The (disabled) ES searches were re-croned to the back of the hour** so that enabling them later does not re-create a burst — the heavy 30-day anomaly searches sit on distinct back-of-hour minutes (`48` / `52` / `56`, daily `03:00`), with the lighter threat-intel / correlation searches phase-staggered.

A cron-expanding collision check (expanding `*`, `*/n`, ranges, and comma-lists over the full hour × minute space) confirms **no two enabled scheduled searches share an (hour, minute)**; a post-deploy scheduler-log check showed **0 skipped / 0 deferred**. AppInspect was re-run (the conf changed) — posture unchanged at 0 errors / 0 failures / 0 future_failures / 8 warnings / 109 success.

> **Note on the plan's daily-band table.** The shipped daily band places the hour-1 spillover at `01:30`–`01:50` (not the `01:08`–`01:30` that an earlier draft used) — the draft's hour-1 minutes overlapped the every-hour aggregate band (`:05`–`:28` fires *every* hour, including hour 1), which would have produced 11 collisions. The corrected layout keeps the entire daily band in the stated `:30`–`:58` window.

### Build 240–241 — Environment Topology detail-tab caching

The Environment Topology view's **graph** (nodes / edges / inventory / Live Activity) was already KV-Store-backed and fast. Its **right-pane detail tabs**, however, were the one corner the dashboard-perf refactor never touched — they dispatched a live raw `sap_logserv_idx_macro` scan on every node/edge click (node Calls/Hr · Top Programs · Errors · Hosts; edge Operations · Performance · Errors), which is multi-second-to-minute at high event volume. Build 240 moves them onto the same hourly KV-Store rollup model as the dashboards.

One new collection, **`logserv_topology_detail_rollup`**, with a `metric` discriminator serves all seven tabs (node `node_hourly` / `node_program` / `node_error` / `node_host`; edge `edge_op` / `edge_perf` / `edge_err`). Edge **Activity** reuses the existing `logserv_topology_edges` rows, and the edge Performance headline p50/p95/max still come straight off the edge row — neither needs a new metric. A new hourly aggregate (`logserv_topology_detail_aggregate`, cron `5 * * * *`), one-time backfill, and 365-day retention search populate and trim it; the **Settings → AI Assistant → Dashboard Data → Run backfill** panel now lists this collection (22 rollups total).

- **Node metrics are byte-exact with the old sourcetype-agnostic OR-match** via an explode-dedup pattern (`scope = mvdedup(mvappend(sap_sid, peer_ip, local_ip, client_ip, clientip, host)) | mvexpand scope`), so an event matching a node in two fields is counted once — exactly as the old `(sap_sid=X OR …)` search did. **Edge metrics** replicate the `aggregate_edges` id derivation (`scope` = the sha1[:16] edge id) so the cache key matches the clicked edge.
- **One intentional display change** (the only non-byte-exact tab): the HANA-tenant edge **Performance** distribution now shows **Avg + Max** of `hana_op_duration_ms` instead of the p50/p75/p95/p99/max percentile breakdown — percentiles don't merge across hourly buckets (Avg = Σsum ÷ Σcount, Max = max-of-per-bucket-max). The headline p50/p95/max still come from the edge row, unchanged.
- **Trade-off:** these tabs are now hourly-fresh (like the dashboards) rather than live; the node Calls/Hr chart's most-recent complete hour lags by up to one aggregate cycle.

All seven tabs were verified byte-exact against the original raw search both pre-deploy (22 node/edge cases) and post-deploy reading the populated collection at 335M events, and live-verified in the browser (the node tabs render the rollup data; the edge reads return correct data from the app's authenticated session; the view stays fully responsive). This build **adds conf** (the collection + transforms + metadata + three saved searches), so AppInspect was **re-run** — posture unchanged at 0 errors / 0 failures / 0 future_failures / 8 warnings / 109 success.

**Build 241 — host-split the node aggregate arms (backfill speedup).** The three full-scan node metrics (`node_hourly` / `node_host` / `node_error`) originally used one explode-dedup arm (`mvdedup(mvappend(6 fields)) | mvexpand scope`) that scans the **whole index** — at 335M a single node_host backfill day measured ~1,059 s. Build 241 splits each into (a) a cheap single-pass `stats … by host | eval scope=host` arm (no `mvexpand`, covering host-nodes) + (b) a small 5-SAP-field explode arm scoped to SAP events only (covering SID/IP-nodes). This is **byte-exact** with the old 6-field OR-match because hostnames are disjoint from SID/IP values (the only double-count case is a single event whose host literally equals one of its own SAP-field values — structurally absent; gated by a 9-case de-risk + the same 22-case post-deploy verify, all byte-exact). The dominant full-index `mvexpand` is eliminated (`stats … by host` ≈ 3 s/day); only the SAP-fields explode arm — a subset of events — remains, so the one-time backfill drops from ~hours to tens of minutes (the steady-state hourly aggregate was already fast and is unchanged). Build 241 is JS-identical to build 240 (only `app.conf` build + the `savedsearches.conf` node arms changed); AppInspect re-run, unchanged at 0/0/0 + 8 warnings / 109 success. The Run-backfill panel still handles any slow arm via its resume-on-reopen mechanism.

### Build 239 — pie/donut empty-after-range-change fix

Every chart applies a vertical color gradient to its bars/slices via a shared post-render SVG walker (`GradientWrap`). It cached each color's gradient `<defs>` id, but didn't verify the gradient still existed in the *current* `<svg>`. When the Splunk Pie viz swaps its `<svg>` (or wipes its defs) on a time-range change, the cached id dangled — slices then painted `fill="url(#missing)"`, i.e. **transparent** — so donut/pie charts (e.g. Change & Configuration Activity → "Change Events by Category") rendered as an empty outline with labels but no colored slices after switching the time range. The initial render was always fine; only re-renders broke.

Build 239 fixes it by deriving the gradient id from a source-hex stashed on each element and re-creating the gradient in the live svg when it's missing (convergent — no DOM mutation when the gradient is already present), plus a `requestAnimationFrame` debounce on the redraw observer as a defense-in-depth cap. This is a JavaScript-bundle-only change (`home.js`); no conf, data-model, or rollup changes, so the AppInspect posture and all rollup verification are unaffected. (Build 238 was an interim attempt at this fix that shipped a quote-parsing bug causing an observer loop; it was reverted and never canonical — preserved only under `testing/iteration_tarballs/`.)

### Latent-bug fixes shipped alongside the refactor

While re-expressing panels, three pre-existing display bugs (all string-vs-number / `match()`-in-base-search predicate mistakes that silently returned 0) were found and fixed:

- **Cloud Connector HTTP Error Rate** (build 204) — the KPI compared a string `is_error` field against the number `1` and was stuck at 0%; re-expressed as the `status >= 400` fraction (now ~7.3% on test data). Build 232 carried this corrected semantics into the Cloud Connector KV-Store rollup (`err_count` where `status >= 400`, ÷ total) when the CIM tier was retired.
- **Change & Configuration "Password Change" / "User Change" KPIs** (build 215) — a Linux `match(_raw, …)` clause sat in base-search position where `match()` (eval-only) silently no-ops; moved to a post-base `| where`. `kpiPassword` 0 → 21 on test data.
- **`icm_is_error` predicate** (build 227) — the same string-vs-number trap (`icm_is_error = 1` vs the EVAL's string `"true"`) in the Environment Health severity rollup and the `logserv_top_error_categories` AI prompt; fixed to `="true"`.

### Backfill button + 365-day cache retention (builds 230–231)

- **"Run backfill" Settings button** (build 230) — a new **Settings → AI Assistant → Dashboard Data** tab seeds all the rollup collections on first install. It dispatches each rollup's aggregation arms as **top-level** searches (immune to the subsearch wall-clock limits that truncate a bundled `| union` backfill at high volume), with a progress bar, completeness banner, and per-rollup status. Idempotent and resumable. **An admin clicks this once after installing on a high-volume instance** to populate dashboard history immediately; otherwise the hourly aggregation seeds history going forward.
- **Cache retention raised 30 → 365 days** (build 231) — all 16 rollup-retention searches (15 dashboard rollups + Topology) now keep a full year. The install backfill still seeds 30 days; the cache then grows to a year organically via the hourly aggregation (seed-and-grow).

The previous canonical App builds are preserved in [`../testing/iteration_tarballs/`](../testing/iteration_tarballs/) (canonical chain: build 233 → 237 → 239 → 240 → 241 → 242 → 243 → 244 → 245 → 246 → 247; build 238 was a reverted interim — an early attempt at the pie fix that shipped an observer-loop bug — kept there for the record but never canonical). The Data TA fsfix tarball is also mirrored there as [`splunk_ta_sap_logserv-0.0.5.0-fsfix-2026-06-01.tar.gz`](../testing/iteration_tarballs/splunk_ta_sap_logserv-0.0.5.0-fsfix-2026-06-01.tar.gz).

## AppInspect

Both tarballs are AppInspect-validated via `splunk-appinspect` in **precert mode with `--included-tags cloud`** (the Splunk Cloud private-app vetting ruleset). To re-run locally:

```bash
pip install splunk-appinspect
splunk-appinspect inspect release_binaries/splunk_app_sap_logserv-0.0.6.0.tar.gz --mode precert --included-tags cloud
splunk-appinspect inspect release_binaries/splunk_ta_sap_logserv-0.0.6.tar.gz --mode precert --included-tags cloud
splunk-appinspect inspect release_binaries/splunk_ta_sap_logserv_azure-0.0.6.tar.gz --mode precert --included-tags cloud
```

Current Cloud-mode posture (AppInspect 4.1.3):

- **App** (build 252): 0 errors / 0 failures / 0 future_failures / 8 baseline warnings / 109 success — see [`appinspect_cloud_logserv_app.json`](./appinspect_cloud_logserv_app.json). Identical gating posture to the v0.0.5.0 App; the dashboard-perf rollup additions through build 252 (KV-Store collections + saved searches + panel-read rewrites + the build-248/249 AI Assistant prompt SPL fixes + the build-249 ES enable + schedule re-stagger + the build-250 MCP time-range fix + the build-251 whole-estate prompt rollup reads + the build-252 react-router bump) add no AppInspect findings. The 8 App warnings are the established baseline (pretrained sourcetypes, public-IP literals in JSON examples, Mako template framework).
- **Data TA** (app version 0.0.6, **no ingest input**): 0 errors / 0 failures / 0 future_failures / 11 baseline warnings / 123 success — see [`appinspect_cloud_logserv_ta.json`](./appinspect_cloud_logserv_ta.json). Same baseline as the prior "fsfix" Data TA — the Azure modular input that an earlier v0.0.6 iteration bundled here (12 warnings / 126 success) has been removed and now lives in the standalone Azure add-on below.
- **Azure TA** (`splunk_ta_sap_logserv_azure`, app version 0.0.6): 0 errors / 0 failures / 0 future_failures / 10 baseline warnings / 111 success — see [`appinspect_cloud_logserv_azure_ta.json`](./appinspect_cloud_logserv_azure_ta.json). The `sap_logserv_azure_queue` modular input is **stdlib-only** (no Azure SDK), so it carries no AArch64 / native-binary / network / credential gating findings; the 10 warnings are all UCC-framework / third-party baseline (SplunkJS telemetry, SDK version, solnlib, Mako template).

All three tarballs pass Cloud-mode precert with **0 errors / 0 failures / 0 future_failures** (the v0.0.5.0 line cleared all prior Data TA Cloud-vetting failures; see the v0.0.5.0 release notes for that history). The App + Data TA install on the Search Head / Deployment Server / Indexer tiers as before; the **Azure TA installs on Heavy Forwarders only** (the data-collection tier — never the Cloud SH or the Deployment Server). The App / Data TA / Azure TA baseline warnings (8 / 11 / 10) are the established UCC-framework + pretrained-sourcetype + public-IP-literal + Mako-template set. Note: AppInspect 4.1.3 reports 220 total checks; an earlier 4.2.0 run reported 242 — the gating posture (0 / 0 / 0) is identical, only the absolute check totals differ by AppInspect version.

Each app also ships a `run_appinspect.sh` helper at `sap_logserv_package/<app>/run_appinspect.sh` that wraps the above.

## Third-party attribution

The App tarball bundles a current CycloneDX 1.4 software bill of materials at `splunk_app_sap_logserv/SBOM.json`, enumerating every third-party dependency with its package URL (purl), version, and hash. It also ships a full attribution document at `splunk_app_sap_logserv/THIRD-PARTY-NOTICES.md` — one section per bundled npm package (1236 packages: name@version, license, repository, and the complete bundled LICENSE/NOTICE text) plus the absorbed-Splunk-add-on attributions. The same file is mirrored at the snapshot root for the GitHub repo; both are regenerated deterministically from the build's `node_modules/` tree by `yarn build` (`bin/generate-third-party-notices.js`), so the listed package versions always match the shipped JavaScript bytes. The Data TA's third-party Python dependencies are pinned in `package/lib/requirements.txt` (notably `solnlib>=5.0.0,<8.0.0` for Splunk Cloud AArch64 compatibility).

## Splunkbase submission status

**Held for further customer review.** The tarballs are ready to ship; submission to the Splunkbase precert API has not been performed and will be initiated by the maintainer who holds Splunkbase credentials. To submit, sign in to <https://splunkbase.splunk.com> and use the **Submit App** UI to upload each tarball as a separate app, or use the Splunkbase REST API with an API key.
