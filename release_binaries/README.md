# v0.1.1 Release Binaries

This directory contains the four installable tarballs for the **Splunk for SAP LogServ** v0.1.1 release: the Search-Head App (**the templates-only build — the LLM path is disabled at compile time**), the Data TA, and the per-Heavy-Forwarder Azure and GCP ingest add-ons. As of 2026-08-18 all four artifacts carry version **0.1.1**.

**The canonical App artifact is now the templates-only build variant** (user direction, 2026-08-18): the same App build compiled with `yarn build:templates-only`, whose compile-time flag **forces the LLM path off at every config-read path** (the build-300 gate — free-form chat disabled with an explanatory banner, provider credentials + model discovery inert, the runtime toggle hidden; the predefined-prompt path via the Splunk MCP Server is fully functional). The **full-LLM build of the exact same source and build number** is preserved in [`../testing/iteration_tarballs/`](../testing/iteration_tarballs/) (`splunk_app_sap_logserv-0.1.1-build333.tar.gz`, md5 `2203f6cfca9658872edea7b1821d6167`) and remains buildable from this source with plain `yarn build` — the two variants' bundles are byte-identical except the compiled flag module and the styled-components build salt (proven per promotion). The v0.1.1 **source** remains the LLM-capable line.

**v0.1.1 is the actively developed release line.** As of App build 245 (2026-07-05) it is at **full feature parity with v0.0.6** — the entire dashboard-performance data-layer rewrite (tstats + KV-Store rollups + the Settings → Dashboard Data backfill panel + staggered schedules), the Azure and GCP ingest add-ons, the three-way Multi-Cloud Overview, the Enterprise Security enable + re-stagger, and every AI Assistant fix (build-248/249 prompt repairs, the build-250 MCP time-range fix, the build-251 rollup-backed host prompts, the build-252 react-router 7.18.1 bump, the build-253 three-way Multi-Cloud dashboard) — while shipping the four vendor LLM providers (Anthropic / OpenAI / Azure OpenAI / AWS Bedrock), free-form chat, Power Mode, privacy tiers, and the Provider Credentials Settings tab. In the **full-LLM build variant** the runtime **`templates_only_mode`** toggle (default **false**) lets an admin restrict a deployment to canned prompts without rebuilding; in the templates-only variant shipped here that state is forced on at compile time and the toggle is hidden. The sibling **v0.0.6 line** (frozen) publishes the LLM-STRIPPED build of the same feature set — the providers physically absent from the bundle, versus this line's functional compile-time disable.

**App build 278 (2026-07-07) additionally ships three v0.1.1-line-only feature arcs** on top of the parity baseline — plus the cross-line **blue-gradient brand refresh** (build 278): the LogServ "LS" app icon recolored from orange to the brand blue gradient (top `#2276CE` → bottom `#0F55A3`, mid ≈ Cisco `#1870C5`) and reshaped to panel-matched corner radii (4 px at the 36 px rendered size — the FramedPanel/KpiCard radius token), shipped consistently across the App, the Data TA, the Azure/GCP add-ons and the Demo Gen TA; and the docs help "?" icon repainted with the same gradient (SVG gradient-stroke square + gradient-fill glyph, 12-second spin preserved, export-capture-safe). The three line-only arcs: the **Cisco Magnetic re-theme** (user-switchable light / dark mode with a nav-bar sun/moon toggle, real Magnetic classic palettes + OneCD teal accent, bundled Inter / Sharp Sans / Roboto Mono fonts, Magnetic component chrome and mode-aware chart palettes across all 21 dashboards + Topology + Settings + AI Assistant; builds 246–259), the **Environment Topology star-system arc** (deterministic star-system Force layout with proportional hub slots + satellite fans, viewport-aware canvas world, Live-Activity collapse/expand graph re-fit, and center-to-center floating edges with bowed curves + arrowheads on the health-ring outer edge; builds 260–274, including the build-271/272 light-mode contrast sweep + occlusion-robust re-fit hardening, the build-273/274 topology PNG/PDF export-fidelity fix — the capture pre-renders the topology svg layers through the browser's native SVG renderer so exported images carry the edges, labels, arrowheads and health rings that html2canvas previously dropped — and the build-277 secondary-SID resize: secondary SAP SID circles enlarged to 90% of the focused-SID size, with edge clipping, layout bounding boxes and collision radii updated in lockstep), and **AI Assistant dynamic model discovery** (builds 275/276 — each provider's model picker now merges a curated static baseline with models discovered live from the configured vendor: Anthropic + OpenAI `/v1/models`, Azure OpenAI deployment discovery, AWS Bedrock `ListFoundationModels`; governed by the default-on `model_discovery_enabled` Settings knob with a "Refresh model list" button, credential-save and 24-hour-TTL triggers; discovered lists cache in the `logserv_ai_models` KV Store collection with a static-baseline floor so a failed refresh never shrinks the picker; every refresh writes a `model_discovery` audit event; refreshed baseline model ids + vendor cost table — unknown model ids report $0, never a guessed price; plus an Azure credential-name fallback (`resource_url` → `endpoint`) that also fixes streaming for Azure configs entered via the Settings page). None of these arcs are in v0.0.6.

**Builds 279–281 (2026-07-08) add the cross-line session-081 UX pass** (shipped to v0.0.6 as builds 256/257): a **Settings readability pass** — 14 px field labels + 13 px hints/intros/subtitles on a wider `clamp(320px, 30%, 520px)` label column across the Settings page, the Dashboard Data panel and the Audit Log viewer; the **Environment Topology Live Activity panel fix** — the panel now fills its zone exactly so its bottom outline completes at any height (it previously clipped mid-row against the zone's `overflow: hidden`), the partners table scrolls internally under a sticky header, the default expanded height grew 220 → 300 px, and the expand gesture floors a smaller saved height up to the default; and the **Sourcetype Mapping link-graph restyle** (Data Pipeline Overview + Host Details, via the new shared `LinkGraphContainer`) — in dark mode the node bars move to readable mid-tones (blue `#4d9dbf`, hover-path purple `#9c80e4`, each with a slight vertical gradient) with dark-grey labels, while light mode keeps the stock fills and gains the same subtle gradient plus white labels. The dark-mode shades were tuned live against the running app and user-approved.

**Build 282 (2026-07-08) adds the cross-line global Cloud Provider filter** (shipped to v0.0.6 as build 258): a persistent **`Cloud: All / aws / azure / gcp`** dropdown in every dashboard's title row (left of the Refresh-interval picker) that filters every panel by cloud provider. The selection is global and per-user-persistent (localStorage `logserv.cloudProvider.<user>`), applying across dashboard navigation and full page reloads. It is intentionally absent on the three views where it doesn't apply — **Multi-Cloud Overview** (already a provider-split view), **Environment Topology**, and **Settings**. Under the hood every non-topology KV-Store rollup grain was extended with a `cloud_provider` dimension (23 rollups × aggregate + backfill searches) and every dashboard read injects the active provider filter (`withCloudProvider` / `mapCloudProviderQueries`); `null`-provider events coalesce to `aws` (the Multi-Cloud convention). A Windows Service-Events / PowerShell source-matching fix (`WinEventLog:System` → also `XmlWinEventLog:System`) is folded in. Live-verified end-to-end on the test fleet: the picker changes every panel (Environment Health Total Errors All 3.80M → azure 1.45M → gcp 185K), persists across navigation + reload, and is correctly absent on the three excluded views; no console errors.

**Build 284 (2026-07-09) fixes two dashboard charts** (shipped to v0.0.6 as build 259): the Linux **"SAP Application Activity"** chart — which rendered 26 long `sap_app` / `sap_sid` combinations as a vertical column chart and clipped every x-axis label to "…" — is now a **horizontal top-15 bar chart** (the combo label sits on the roomy y-axis) via a new `chartType="bar"` on the shared chart component; and the Windows **"Critical / Error"** KPI + **"PowerShell Activity"** chart — empty because of query mismatches, not missing data — now read the already-indexed data: the PowerShell chart matches any PowerShell source (`(?i)powershell`, since the demo indexes them under `XmlWinEventLog:Microsoft-Windows-PowerShell/Operational`, not `WinEventLog:Powershell`), and the Critical/Error KPI counts CIM `severity IN ("critical","high")` (Splunk_TA_windows maps the Windows Error level → `high`). Both are pure webapp-query changes. The demo test data on the fleet also gained ~457 genuine Level-1 Critical Windows events (Kernel-Power 41 + WHEA-Logger 18) so the `critical` severity bucket is non-empty. User-validated on the test fleet.

**Build 285 (2026-07-09) converts the AI Assistant's high-volume predefined prompts to the KV-Store rollup tier** (shipped to v0.0.6 as build 260): twenty-three prompts that raw-scanned high-volume sourcetypes (linux_messages / ICM / DNS / Web Dispatcher / squid / work-process / Windows) exceeded the AI Assistant's 30-second MCP dispatch timeout at wide time ranges on high-ingest environments (“signal is aborted without reason”); they now read the same hourly KV-Store rollups as the dashboards and complete in well under 2 seconds even at 30 days over a 450M-event dataset. Five new rollup metric arms back the prompts that had no existing rollup dimension (linux `pam` + `cgsfw`, wp_perf `wperr`, proxy `denieddest`, windows `logon`) — the Settings → Dashboard Data backfill covers them automatically, and the existing metrics' row keys are byte-identical under the new metric-scoped `_key` branches (no re-backfill needed on upgrade). Three prompts intentionally report differently: Web Dispatcher slow-URIs ranks by avg+max response time instead of p95 (percentiles don't roll up), DNS beaconing uses the dashboards' gap-variance detection (daily granularity), and the cross-stack auth-failure prompts adopt the Cross-Stack Authentication dashboard's canonical failure definition. Intent map at v0.0.12 (conf + intent-map SPL byte-synced). Twelve equality-comparable conversions verified row-identical vs their raw predecessors, and all 23 dispatch-verified by name on both test hosts (worst 1.75 s at -30d, zero failures).

**Build 286 (2026-07-09, same session) fixes the follow-up user reports** (shipped to v0.0.6 as build 261): four trend prompts rendered all-zero charts because the MCP server caps tool responses at its default 100-row limit keeping the FIRST rows — an hourly 30-day trend (721 rows) therefore displayed only its oldest ~4 days. The ten >100-row time-series prompts now bin adaptively (`bins=90 minspan=1h` — identical hourly behavior at short windows, ~30–90 complete-window points at wide ones; totals verified conserved exactly). Two ES prompts still timed out and are fixed: the after-hours-admin prompt's web-traffic join subsearch reads the sourcetype-mapping rollup (verified row-identical; 18.8 s at -30d, was aborting), and the web-dispatcher response-time anomaly is re-expressed on a new per-host hourly `wd_host` web-timing rollup metric as an avg+max z-score (percentiles don't roll up; 0.15 s, was 154 s+ — its daily scheduled detection becomes sub-second too, and its notable/risk templates + correlation-search metadata were updated in lockstep). Intent map at v0.0.13. All 12 fixed prompts re-verified by name on both hosts with recent-row VALUE assertions (the round-1 verify only checked row counts — the gap that let the truncation bug through).

**Build 287 (2026-07-09, same session) makes the AI Assistant's MCP request timeout admin-configurable** (shipped to v0.0.6 as build 262): the browser-side timeout that aborts a slow MCP request (tool dispatch, saved-search run, health probe) with `signal is aborted without reason` was a hardcoded 30 s; it is now the `mcp_timeout_seconds` setting (default 30, range 5–600), editable at **Settings → AI Assistant → General → MCP request timeout (seconds)**. MCPClient resolves it per-request from the KV-Store settings row exactly like `mcp_server_url`. It is the browser-side abort; the MCP server (App 7931) has its own separate `mcp.conf [server] timeout` (60 s default), so the effective ceiling for any one request is the lower of the two. Config-plumbing verified on both hosts (conf default 30, KV-Store round-trip of the field).

**Build 288 (2026-07-09, same session) adds a read-only display of the MCP server's OWN timeout** (shipped to v0.0.6 as build 263): beside the client `mcp_timeout_seconds` field, Settings → AI Assistant → General now shows App 7931's `mcp.conf [server] timeout` (read cross-app), so an admin sees both numbers and the effective-ceiling-is-the-lower relationship at a glance. It is display-only by design — a cross-app conf write needs `admin_all_objects` (fails for sc_subadmin on locked-down Cloud) and, because the MCP handlers are persistent processes (`scripttype = persist`) that cache settings in a singleton, wouldn't take effect until a Splunkd restart anyway; the server timeout stays edited the normal way (that app's `mcp.conf` + restart). The row degrades to "Not detected" when App 7931 isn't installed / reachable. Verified on both hosts (cross-app read returns 60s).

**Build 289 (2026-07-09, same session) changes the MCP request timeout DEFAULT from 30 to 60 seconds** (shipped to v0.0.6 as build 264): a fresh install / unset `mcp_timeout_seconds` now defaults to 60s — aligning the client default with the MCP server's own 60s default so the client no longer aborts before the server would. Changed in three places (the conf default, `DEFAULT_AI_CONFIG`, and MCPClient's read-failure fallback). An admin's explicit saved value still wins (the default only applies when the field is unset). Verified on both hosts (conf default 60; jaclyn's unset KV field now resolves to 60; sh-idxr keeps its explicitly-saved value).

**Builds 290–298 (2026-07-10/12) complete the sub-hour hybrid time-range rollout and add a global Refresh button + a WebDispatcher cloud-filter fix** (shipped to v0.0.6 as build 265). The dashboards read hourly-aggregated KV-Store rollups keyed on `bucket_ts`, so a **sub-hour** time range (Splunk's picker offers "Last 15 min" / "Last 60 min" / custom real-time one click away) read a rolled-up panel **empty** (a window that falls inside one hour matched no aligned bucket) or **~4× over-counted** (a window that crossed an hour boundary matched the whole hour). Builds 290–296 close that gap across **every** rollup-backed panel: a new span-based router (`utils/hybridRouting.ts`, 90-minute threshold) + hybrid search hooks (`hooks/useHybridSearch.ts`) route a window narrower than 90 min to the panel's original **raw** SPL (exact at any sub-hour range) and a wider window to the **hourly cache** (fast). Coverage is all 21 data dashboards + both Sourcetype Mapping link-graph panels + the Host Details Role Activity tab; intentionally-cached panels stay cached (cosmetic `span=1d` sparklines, the streamstats beaconing-detection panels, and the `| head N` event-listing tables — none of which have a meaningful sub-hour reading). Each hybridised panel's raw arm was reconstructed from its rollup's aggregate SPL and **byte-verified cached-vs-raw at 450M events** (a 23-agent read-only audit confirmed no rollup-backed panel was missed). **Build 297** adds a **global Refresh button** to the nav bar (right of the time-range picker, on every page) — a manual-refresh nonce at the AppShell level that re-runs the current view's panels for the selected range; it coexists with the per-dashboard auto-refresh interval and per-panel refresh. **Build 298** fixes the **WebDispatcher cloud-filter gap**: three panels (Top URIs, Traffic by Status Code, Response Time Trend) built their SPL inline and bypassed the global Cloud Provider filter, so a selected provider left them showing every provider's data; they now route through `withCloudProvider` (a no-op for `Cloud: All`, so the default view is unchanged). User-visual-pass-confirmed on the test fleet.

**Build 299 (2026-08-07) fixes the after-hours window, which was a silent no-op product-wide** (shipped to v0.0.6 as build 266). The `[sap:hana:audit]` props.conf calculated fields chained one EVAL onto another (`hour_of_day` derived from `EVAL-audit_datetime`), which Splunk does not reliably resolve — so `hour_of_day` and `day_of_week` measured **0% populated** and `is_business_hours` was pinned to the dead constant `"false"` on every event. Two opposite failure modes followed, decided purely by quoting. The HANA Audit **"After-Hours / Weekend Admin Activity"** panel compared against a bare `false`, which in a `where` clause parses as a *field reference*, so it matched nothing: the filter degenerated to weekend-only, **omitting every weekday after-hours event** (the security-relevant slice) and mislabelling all rows "Weekend". The Change & Configuration Activity **After-Hours Changes** KPI and its rollup used the quoted form, which matched the constant and flagged **100% of HANA events** as after-hours. Both are fixed: props.conf now derives each field inline from `_time`, and every consumer (the HANA panel, the ChangeConfig `ENRICH` classifier, and the `logserv_compliance_aggregate` / `_backfill` rollup arms) computes the window inline rather than reading a search-time field — an invisible dependency for a security filter that fails silently in both directions. One canonical window applies to all three change sources: **business hours 08:00–18:59, after-hours = hour < 8 OR hour > 18 OR weekend** (previously HANA delegated to the dead fields while Windows/Linux used a 07:00–19:59 window). Verified on 106M events: the corrected HANA panel query matches an independently-formulated reference exactly (980,650), recovering **540,185 weekday after-hours events** that were previously invisible, and the rebuilt rollup matches raw exactly on every source. Existing installs should re-run the compliance rollup backfill — `is_after_hours` is part of the rollup's `_key`, so historical rows carry the old classification. Two deliberate exceptions are documented and unchanged: the three ES off-hours correlation searches (`hour < 8 OR hour >= 18`) and the `logserv_hana_after_hours_admin` prompt (`hour < 6 OR hour >= 20`, a deep-overnight tripwire that states its own window).

**Build 300 (2026-08-07) makes `yarn build:templates-only` actually disable the LLM path.** The compile-time `TEMPLATES_ONLY` flag existed and was correctly wired (`bin/build.js --templates-only` → webpack DefinePlugin → `pages/home/buildFlags.ts`), but session 030 had moved the user-facing gating to the **runtime** `templates_only_mode` setting — so the flag only gated model discovery and two Settings rows, and a templates-only build still had **working free-form LLM chat**. The flag now *forces the runtime value on* at the config-read chokepoint (`utils/aiConfigApi.ts`): `parseRawContent` is the single normalizer for **both** storage sources (the KV-Store settings row and the conf-file stanza) and `DEFAULT_AI_CONFIG` is the both-reads-failed fallback, so every read path reports `templates_only_mode: true` regardless of what is stored — which the existing runtime machinery then acts on (chat input read-only, Send disabled, `sendUserMessage` short-circuits with a system notice, model picker + Power Mode hidden, Provider Credentials tab hidden, explanatory banner). Patching only the shipped conf default would **not** have been sufficient: **KV Store wins over the conf**, so a customer upgrading from a full-LLM build — whose KV row holds `templates_only_mode = 0`, exactly the state the test fleet is in — would have kept the LLM path enabled. Also forced: the pre-load default (`state/AIAssistantConfig.ts`), so the window before the async config read resolves cannot present an LLM-enabled UI, and the `sendUserMessage` guard itself (defense in depth, independent of the prop chain). The Settings **"Templates-only mode"** toggle is hidden in such a build — it could not turn the LLM path back on, so rendering it would be a control that lies — and `bin/build.js` patches the **staged** conf default to `true` (tolerant regex + self-assert that fails the build on formatting drift; the source conf is never touched) so the artifact is self-describing to `btool` / the REST conf endpoint. Per the standing decision the vendor provider modules are **not** stripped from the bundle: this is a functional disable, so vendor endpoint strings remain in `home.js`. **The full-LLM build is provably unchanged** — the two variants' `home.js` are the same length and differ in exactly two regions: the compiled flag module (`{F:()=>r});var r=!0` vs `var r=!1`) and styled-components' known per-build salt. The templates-only variant of this build is preserved in [`../testing/iteration_tarballs/`](../testing/iteration_tarballs/) as `splunk_app_sap_logserv-0.1.1-build300-templates-only.tar.gz` (md5 `650cd4e62564c22bbec17e0aa1271953`); it is a **build variant, not a separate canonical** — the canonical v0.1.1 App is and remains the full-LLM build.

**Build 301 (2026-08-07) removes a stale prompt count from a user-visible string.** The AI Assistant's templates-only banner read *"…run any of the 48 saved searches…"* while the catalog holds **61** prompts (verified from the intent map: 15 SAP Basis / 28 Security / 18 Operations). The number is now gone rather than corrected — the catalog has grown 40 → 42 → 48 → 61 across releases and a hard-coded count in a user-visible string silently rots. The same stale `48` was swept from four further places: the shipped `default/ai_assistant_settings.conf` comment (admin-visible via `btool`), two rows in the developer AI Assistant internals reference, and the architecture page's predefined-prompt description. All are now number-free. No behavior change.

**Builds 302–303 (2026-08-07) add an About dialog with the version, build number and build date.** A new **About** item sits in the navigation bar to the right of *Platform*; clicking it opens a dialog showing the app icon, the product name **Splunk for SAP LogServ**, and the running **version**, **build** and **build date**, with a Close button (backdrop click and Escape also dismiss). The values are **derived, not typed**: webpack reads `[id] version` and `[install] build` from the shipped `default/app.conf` at compile time and stamps the compile date, baking all three into the bundle via DefinePlugin (surfaced as `APP_VERSION` / `APP_BUILD` / `APP_BUILD_DATE` in `buildFlags.ts`). The build date is UTC **date only** (`YYYY-MM-DD`), deliberately: same-day builds stay byte-comparable — distinguishing two builds from one day is the build number's job, so the dialog always describes the installed bundle and cannot drift the way the hard-coded prompt count fixed in build 301 did; a missing value fails the build rather than shipping an empty dialog. The app icon is now also shipped under `appserver/static/` (Splunk Web serves that tree at `/<locale>/static/app/<app>/`; the top-level `static/` copy the launcher reads is not web-served) — the same URL derivation the bundled web fonts already use.

**Builds 306–313 (2026-08-07/09) add the LogServ Data Doctor — the missing-data diagnostic.** Every empty panel now explains itself, and the explanation can escalate to a support-ready report — runnable by a **non-admin**, with no destructive controls anywhere. **306**: every empty chart/table states *why* via free checks that dispatch no searches (an active Cloud/host filter, a sub-day window on a once-a-day summary, the panel's own search error) instead of the often-wrong "in this time range" boilerplate; KPI cards show a compact form (e.g. *"Filtered: Cloud=GCP"*) with the full sentence in the tooltip; building the check census also surfaced **four live SPL defects, all fixed** — a base-clause `match()` that silenced half of the HANA privilege-escalation correlation search, the last surviving `icm_is_error=1` string-vs-number comparison (Environment Health's Total Errors drilldown), and two never-extracted field names (`audit_action`, `action_status`) that undercounted the Environment Health Auth Failures KPI by 3% (all HANA failed CONNECTs) and left the topology HANA-audit edge Performance tab empty — the affected rollup arms were re-run and verified exact against raw. **307**: a *"Why is this empty?" / "Run full diagnosis"* drawer runs an ordered, budget-bounded evidence cascade (search-head canary → index visibility from the macro definition → index presence → sourcetype probes → summarised-data extent) whose Gate 0 can give the most valuable honest answer: *"there genuinely are no such events — nothing is broken."* **308**: panels that read summarised data are traced to the aggregate saved search that populates their collection, so the drawer distinguishes *"the underlying events exist but were never summarised"* (with the Settings → Dashboard Data backfill pointer) from *"the source events genuinely don't exist."* **309**: a verdict-honesty pass — probe failures are never presented as evidence of absence, a quiet window is never a confirmed "stopped arriving", a daily summary is never "stale" at its natural cadence, and new hedged/expected verdicts (`rollup-leading-edge`, `rollup-metric-empty`, `rollup-window-uncovered`, `sourcetype-outside-window`) replace five classes of false confirmed faults. **310**: branded **Data Doctor PDF reports with a machine-readable `.json` twin** at all three levels — the drawer's Download PDF (panel), Actions → *Diagnose dashboard (PDF)* (free checks on every registered search + the full cascade on the empty ones, shared probes memoized), and Actions → *Environment report (PDF)* (canary + platform facts + macro/index/sourcetype presence + per-rollup health). **311**: the **Platform → Diagnostics page** (`#/diagnostics`) renders the live environment view — summary facts, a per-rollup table with independent **Freshness** and **History** verdicts that agree with Settings → Dashboard Data, sourcetype presence, and the full checks ledger — re-runs on time-picker changes and the global Refresh, supports Cancel with honestly-labeled partial results, and every downloaded report also persists to the new **`logserv_diag_reports`** KV Store collection, listed on the page for re-download (re-downloads never re-persist). A nightly retention search (`56 1 * * *`, collision-free against all 77 enabled scheduled searches) keeps the newest 100 reports within 365 days and carries `override_if_empty=false` so a KV warm-up blip can never wipe the only non-re-derivable collection (proven live). **312**: the session-097 **rendered-UI pass caught the page's completion handler releasing the diagnosis singleton BEFORE reading its cancellation state** — `endDiagnosis()` cancels the runner as part of the release, so every completed run read as cancelled, was misclassified as an outside supersede, and the page could never render evidence (a defect invisible to the REST/gate-level verification, which is exactly why the rendered pass exists); fixed by capturing the state before the release, with the ordering now pinned in the build gate (mutation-kill-tested in both directions). The full validation-plan section-G pass then completed live on the test fleet: auto-run + progressive labels, the 27-row rollup table with independent Freshness/History verdicts agreeing with Settings → Dashboard Data, picker-change and global-Refresh re-runs with window-bound evidence labels, Cancel → honestly-labeled partial (never counted into the warn banner), cross-surface supersede (page notice + kept evidence while the Actions report completed), download→persist for all three report scopes, re-downloads that never re-persist, the help icon, and zero destructive controls — with a zero-error console under instrumentation. The whole arc is build-gate-enforced — splProbe 86 · panelDiagnosis 164 · diagCascade 177 · diagEvidence 18 · diagReport 46 · diagPersistence 68 checks plus the aggregate-scope trace, conf-drift and completion-path-ordering checks, with the worst failure modes mutation-kill-tested. **313**: the fresh-install correctness pass from the session-097 coverage review — five new verdicts (routing-not-applied with a routed-events-zero discriminator; rollup-source-never-seen, which stops prescribing a backfill when the feed has never arrived; extraction-app-missing for Windows panels without Splunk_TA_windows on the search tier; kvstore-not-ready for mongod warm-up; sourcetype-future-timestamps for clock/timezone misconfiguration), the index-authorization verdict naming every cause its evidence cannot separate, never-arrived claims scoped to the probed index, the producer-disabled upgrade of the stale-summary verdict, the Data TA added to the companion-apps check (with its expected-tier note), and real panel titles in saved reports — all adversarially design-reviewed before implementation (design SS14/SS14.8a; 1 blocker + 7 highs folded in), with the cascade suite grown to 245 checks + a 135-combination invariant sweep and three further kill-tested mutations.

## Canonical tarballs

**App build 334 (2026-08-19) — string-accuracy pass from the zero-trust docs audit.** Eight code-side string/label defects surfaced by the session-114/115 audit, all string/comment/conf-level with no logic change: the three Settings-page UI strings (and two shipped-conf comments) that still named the audit index by its retired pre-rename form `ai_assistant_audit` now say `logserv_ai_assistant_audit` (an admin following the HEC-token hint would have granted write permission on an index the app does not use); the Environment Topology Live Activity caption now reads “Top N busiest edges” — the table lists the busiest edges of ANY integration type, not just RFC; the Network Perimeter activity chart no longer claims a log-scale y-axis it never had; the Host Details “Windows Event Codes” subtitle no longer claims a top-15 cap that does not exist (the table paginates all event codes); the ES Risk Notable's `description` (visible in ES Content Management) now says it aggregates the SAP-side detection searches rather than “the 5 correlation searches” — its SPL reads all of them; the two behavioral-anomaly notable descriptions now describe the daily scan of hourly buckets since the start of the previous day instead of “the most recent hour” (stale since the build-316 re-cron — this is the text a SOC analyst reads on the notable); the templates-only Settings banner no longer instructs admins to “toggle templates-only mode off” in a build where the toggle is hidden and the mode is fixed at compile time (the sentence now branches on the build flag); and `collections.conf` types the `mcp_timeout_seconds` field (`number`) so the setting round-trips typed like its sibling fields.

**App build 333 (2026-08-19) — elkjs license attribution restored.** The Environment Topology layout engine `elkjs@0.11.1` (EPL-2.0) has been bundled since the topology's ELK layouts shipped, but was never declared in `package.json`/`yarn.lock` (the long-standing lockfile-drift gotcha), so the generated `THIRD-PARTY-NOTICES.md` — which walks the resolved workspace `node_modules` — omitted its entry and license text in every prior App tarball. Build 333 declares the dependency properly; the shipped notices now carry the `elkjs@0.11.1` section with the full EPL-2.0 text (1236 packages walked, up from 1235). No functional change — the bundled elkjs bytes are identical — and the session-021 `d3-color >= 3.1.0` resolution was re-verified intact after the dependency add. Found by the session-114 zero-trust docs audit.

**The 2026-08-18 promotion (App build 332 templates-only + all three TAs rebuilt at version 0.1.1).** Per user direction the canonical App tarball switched to the **templates-only build variant** of build 332 (see the framing note at the top of this README — the full-LLM variant of the same build is archived beside it), and the three TAs were **rebuilt as canonical version 0.1.1 artifacts**: the Data TA (already versioned 0.1.1) was rebuilt from the current source — functionally identical to the prior blue-icon canonical, the member-diff showing only the build stamp, the restmap `python.required`/`passSystemAuth` fields repositioned within their stanza by the session-091 multi-stanza patcher, and a benign `packaging` 26.2 → 26.3 transitive-library refresh — while the **Azure and GCP ingest add-ons moved from their line-independent `0.0.6` versioning to `0.1.1`**, aligning all four artifacts on the release-line version (their tarball FILENAMES change accordingly; content otherwise identical to the 0.0.6 blue-icon builds apart from the version stamps and the same `packaging` refresh). All four artifacts re-ran AppInspect Cloud clean at their historical baselines the same day.

**Builds 329–332 (2026-08-18) add IP-endpoint name enrichment, a top-5 High Traffic cutoff, decluttered donut legends, and copy/open-in-search actions on the Data Doctor's rollup SPL.** **Build 330** (329 was superseded pre-deploy by a saved-search prefix fix): the topology's square partner nodes labelled with raw IP addresses can now say **who and what is behind the address** — up to two extra lines under the IP (the machine's **hostname** and a **user line**: the name when exactly one distinct user was seen from that address, a count like "7 users" otherwise) plus Overview rows (**Hostname** with its evidence source, **Users** grouped by source, **Names as of**), fed by a new daily `logserv_topology_ip_enrichment` KV rollup built from same-event evidence only (HANA audit client registrations, Windows 4624 logons with the workstation-names-the-local-machine guard, SAP start service auth lines, Linux sshd sessions; gateway logs deliberately excluded — their peer-IP and hostname lines never co-occur; ambiguity suppresses rather than guesses). **Build 331**: the **High Traffic SID** classification tightened to the top **five** systems by total incident calls (was ten), and the partner-donut legend rows dropped their ownership badge + footnotes (the graph folds every inventory-attributed address into its owner's node, so "owner not established" was the only verdict a legend row could ever show). **Build 332**: in the Data Doctor drawer's *Rollup-populating saved searches* section, each printed definition gains two corner actions — **copy to clipboard** (verbatim) and **open in the Search app** in a new tab scoped to the diagnosed window, with the terminal `| outputlookup` write **removed first** (run verbatim over a partial window, an aggregation search would upsert partial bucket rows over correct summary rows; the drawer never offers a control that can change data — a build-time check proves the removal against every shipped aggregation search, and when the write cannot be removed with certainty the open action is not rendered at all). Hold **Shift and drag** on empty canvas to draw a selection box — every node it touches joins the group and gains a **dashed outline** (deliberately neutral ink, so it can never be misread as a health signal or an edge colour), a status chip at the canvas top-right shows the count, and **dragging any highlighted node moves the whole group rigidly**, edges following their nodes. Shift/Ctrl/Cmd+click toggles a single node's membership (never opening the Details panel); a plain click inspects a node and releases the group; Escape or an empty-canvas click clears it; arrow keys nudge a focused group; with Snap mode on, release applies *one shared* grid offset so the group's shape is preserved. Group moves mark the layout Unsaved and are captured by Save Layout — including keyboard nudges, which previously bypassed dirty-tracking even for single nodes — and the group survives data refreshes and time-range changes. Plain drag still pans; nothing about the existing gestures changed. The change also retires two latent hazards: the canvas library's **Backspace-deletes-selected-nodes** default is now explicitly disabled, and its built-in selection rectangle — which silently swallowed every click over the selection's bounding box — is suppressed in favour of the per-node outlines. Adversarially design-reviewed against the installed canvas-library source before implementation (3 lenses, 33 findings including 3 blockers, all folded), gate-pinned (14 new build-gate pins, 7 mutations kill-tested), and rendered-pass-verified live at every step: rigid multi-node moves exact to the decimal, snap producing one shared offset with the grabbed node landing on-grid, Backspace deleting nothing, and the selection surviving a data refresh. **Build 327** fixes a build-324 regression the rendered pass surfaced: the HANA-tenant circles' bottom chip ("HANA TENANT · n") sat at the shared 2 px margin and the health ring's bottom arc cut straight through it — tenants are the one variant whose name renders inside the disc, so the chip is the first element below the ring; it now clears at the same 22 px as the SID label. **Build 328** fixes a defect of the new feature itself, also caught by the rendered pass: Splunk Web keeps a permanently-mounted *hidden* "Disconnected from Splunk server" dialog in the DOM, and the Escape handler's any-dialog guard treated it as an open modal — so Escape never cleared the group; only a *visible* dialog now owns Escape.

**Build 325 (2026-08-13) adds host + instance visibility and fixes a latent RFC undercount.** Hovering a SAP system circle now shows a **Hosts** row (distinct hosts that logged its events in the selected window), the node Overview gains a **"Hosts (in range)"** fact (on a HANA tenant node the count is scoped by the tenant's shared name and the panel says so inline — and the tenant's hover tooltip deliberately carries no Hosts row, since a bare name/value row cannot carry that hedge), and the Hosts tab lists the SAP **instance numbers** seen on each host (`inst 00, 01`). All three read the same hourly rollup as the Hosts tab itself, so the numbers agree; the bulk host-count read lands a few seconds after the graph renders and is delivered outside the layout pipeline, so its arrival never re-arranges the layout or resets the viewport (isolation-verified: zero transform/node movement across the landing), and a time-range change empties the rows while loading rather than presenting the previous window's numbers as current. **RFC rollup rows are now keyed per SID-side gateway listening address** (`local_ip`), fixing a latent last-write-wins collision in which a multi-app-server SID's RFC call counts undercounted whenever two of its app servers served the same partner in the same hour; the Edge Details Overview gains a **"By app server"** table splitting each RFC edge's calls and errors by app-server address with inventory owner badges, its rows partitioning the edge's calls so they sum to the *Calls in window* fact above them. The last hard-coded SID seed is also gone from the aggregation searches — stored rows are classification-neutral. **Upgrading an existing install:** the per-app-server change re-keys the topology graph rollup's RFC rows — run **Clear** then **Backfill** once on *"Environment Topology (graph)"* in Settings → Dashboard Data (the panel carries the note, including why a Backfill without the Clear double-counts, why the completeness check would otherwise skip it, the >30-day history-loss caveat, and the release-notes pointer to the surgical RFC-only migration that keeps the rest of the history). Adversarially reviewed before implementation (three lenses, 3 HIGHs folded — the host-count delivery path, the tenant hedge, and the migration warnings); the sh-idxr migration verified byte-exact (RFC rollup == raw at 177,788 calls / 32,831 errors, per-app-server split exact, non-RFC rows untouched).

**Build 324 (2026-08-13) makes the SID classification data-derived and puts an icon in every system circle.** The classification formerly labelled "Focused SAP SID" is now **High Traffic / Regular Traffic**: the view ranks SAP systems at render time by total calls touching them (inbound + outbound + bidirectional) and marks the top ten High Traffic — only SAP systems compete for the slots (a high-volume client IP can no longer occupy one), and on estates with ten or fewer systems every SID is High Traffic, by design. Both kinds render at the same large size (100 px disc + health halo). Every SAP system circle carries an interior icon — an **app-server rack** for application systems, the **database cylinder** for database systems — and HANA **tenant databases** render as full-size circles with a combined app-servers + database icon, tagged "APP SERVERS - HANA TENANT" (chip: "HANA TENANT"). Horizontal dividers now separate every section in the right panel's tabs. Saved layouts may need a one-time re-save after the geometry change. Visual-pass-confirmed on the test fleet (2026-08-14): icons, rings and arrowheads-on-ring verified in close-up, tooltip/panel labels exact, and the console clean throughout.

**Build 323 (2026-08-12) completes the partner charts.** The legend beneath each chart now lists **every** partner with no cap and no remainder row — build 322 had briefly bounded it at eight rows to keep the two stacked blocks short, and nothing about a node's traffic should be left unnamed on a panel whose whole purpose is naming it. Long endpoints wrap rather than being cut off, and the exact call count still holds its place in the row. **Hovering any slice** now gives that endpoint, its exact call count, and its share of that chart's total — which matters because a slice too small to be visible is deliberately drawn a little larger, so the angle alone cannot be trusted to convey the share. That approximation is now disclosed exactly when it happens rather than when a partner count crosses a threshold. Three build-time checks were added and mutation-tested so the cap cannot return and the tooltip cannot be dropped.

**Build 322 (2026-08-12) answers three questions the Environment Topology right panel could not.** A customer asked which of the IP addresses on a system's panel are its own instances, whether the "Top partners" donut counted inbound or outbound traffic, and what relationship the Hosts tab's rows have to the selected system. The panel had no answer to any of them, and one of the three premises turned out to be wrong. **Which IPs are mine? None of the visible ones** — an address the inventory attributes to a system is folded into that system's node before the graph is drawn, so an address still shown separately is one no system has claimed. Every endpoint now carries that verdict as a badge (`owner: XCP` / `owner not established`), classified inside the data layer from the node's identity rather than its rendered name, with the provenance stated: ownership comes from an inventory that is not limited to the selected time range. **Inbound or outbound? It was both, summed.** It is now two donuts, one per direction, each listing *every* partner — no "OTHER" bucket — with exact per-direction totals added to the facts table so the two reconcile against the headline. Edges the data records as *bidirectional* are counted on their own line rather than being assigned a direction the stored row denies. **What are the Hosts-tab rows? The system's own hosts** — so the requested inbound/outbound column would have been meaningless there, and a per-host traffic table sits beneath it instead. That table names a receiving host wherever the data supports one and falls back to an edge-type row where it does not, so it accounts for 100% of the node's calls and is checkable against the same headline. The Hosts read was also **silently capped at 20 rows** while the caption reported that cap as the host count; the cap is now 100 and, when reached, said out loud. Three donut-rendering defects the previous four-partner cap had been hiding are fixed: sub-1% wedges rendered as background while their legend row claimed a colour, a single-wedge donut drew nothing at all, and an empty one asserted "0 calls" as a measurement. Adversarially reviewed twice — once before implementation (three lenses, all DO-NOT-SHIP, 43 findings including 7 blockers, all folded; the headline one was that the requested per-host inbound/outbound column can never render an outbound row, because a hostname is only ever the receiving end of a call) and once over the finished diff (three lenses, all SHIP-WITH-CORRECTIONS, 12 findings folded — two panel sentences that were false on a tenant-database node, six build-time checks that survived their own mutations, and a palette that emitted duplicate colours past 28 wedges). Verified against 106M live events: every node's traffic rows sum exactly to its rendered call total, the three-way direction split sums to the same number, and on the very node the design used as evidence the old cap was indeed hiding a host. Twenty mutations of the new logic were each confirmed to fail the build.

**Build 321 (2026-08-12) makes the Environment Topology Edge Details tabs work again — they had dispatched nothing since build 240.** Selecting an edge showed "no events for this edge in the current time range" on all four tabs, for every edge, on every time range. The rendered edge's key is a composite of its two (retargeted) endpoints and its type; the searches behind the tabs accept only the *stored* edge id (`substr(sha1(source_id . ":" . target_id . ":" . type), 1, 16)`), so the id-validation whitelist rejected every read before it ran and `useSearch` was never enabled. The Overview tab's headline figures come from data already in the browser, which is why the pane still looked alive — and the session-060 verification had exercised the reads by issuing their SPL manually with a real id rather than through the UI, so nothing caught it. The stored ids were in memory the whole time and simply never carried onto the edge. **The fix** adds a required `bucketIds` to the edge (a *set*, because inventory retargeting can collapse several stored edges into one on the canvas) and routes the three rollup reads through it, splicing every id as an equality disjunction. Validation now **fails closed**: one malformed id rejects the whole selection rather than silently querying a subset, and if an edge ever spans more underlying edges than a single read may address, the tab says *"showing the first N of M"* instead of presenting a partial total as a fact. **The Activity chart is no longer a search at all** — it is computed from the same rows that produce the Overview totals, so its "successful calls + errors" legend is an exact decomposition of "calls in window"; previously the two resolved slightly different windows (Splunk's `addinfo` calendar arithmetic in the search head's local timezone with an exclusive upper bound, versus the hook's UTC-snapped inclusive one). **Empty tabs now distinguish "queried, found nothing" from "nothing was queried"**, which is the ambiguity that let an 80-build outage stay invisible. Three labels were corrected on surfaces this made visible again: the HANA Tenant performance chart is titled mean and maximum rather than percentiles (percentiles cannot be merged across hourly buckets — the read has emitted `Avg ms` / `Max ms` since build 240), the HANA Audit Overview row reads "Auth failures (CONNECT)" to distinguish it from the broader Errors tab, and two RFC performance cells that no saved search has ever populated were removed. Adversarially reviewed before implementation (three lenses; two returned SHIP-WITH-CORRECTIONS and one DO-NOT-SHIP, 42 findings including 4 blockers, all folded). Verified: the shipped builders executed against 106M live events return rows for every edge type and tab (http 10/3/7, HANA Tenant 5/2/1, HANA Audit 8/1/0, RFC 0/0/1 — RFC's Operations and Performance are legitimately empty, `icm_program` and `icm_tasks` being absent from gateway events), union arithmetic is exact, and the Performance histogram's 4xx+5xx equals the edge's headline error count. A build-time check now derives the stored id's shape and length from the shipped searches and pins both call sites, so the two halves of this interface cannot drift apart again; nine mutations of the new logic were each confirmed to fail the build.

| Tarball | md5 | Size | Tier |
|---|---|---|---|
| [`splunk_app_sap_logserv-0.1.1.tar.gz`](./splunk_app_sap_logserv-0.1.1.tar.gz) | `3f9c77583ee8de6f201a8a327cc38420` | 4.71 MB | Search Head — **build 334, templates-only variant** (LLM path compile-time disabled; the full-LLM variant of the same build is archived in `../testing/iteration_tarballs/`) |
| [`splunk_ta_sap_logserv-0.1.1.tar.gz`](./splunk_ta_sap_logserv-0.1.1.tar.gz) | `b87e00de84d89c47509e7d820ff53278` | 1.58 MB | Deployment Server + Heavy Forwarders + Indexer (also installable on Splunk Cloud SH for single-instance Cloud deployments) |
| [`splunk_ta_sap_logserv_azure-0.1.1.tar.gz`](./splunk_ta_sap_logserv_azure-0.1.1.tar.gz) | `ffb8bb15625262c93b1c990c57ba54b8` | 1.57 MB | Heavy Forwarders only — Azure ingest add-on; **install per-HF, NOT via the Deployment Server** (only needed for Azure Blob ingest) |
| [`splunk_ta_sap_logserv_gcp-0.1.1.tar.gz`](./splunk_ta_sap_logserv_gcp-0.1.1.tar.gz) | `93306c0c7c45c175835f2342cdf8d07e` | 1.57 MB | Heavy Forwarders only — GCP ingest add-on; **install per-HF, NOT via the Deployment Server** (only needed for Google Cloud Storage ingest) |

The **App tarball is at build 332, in the templates-only variant** (internal `app.conf` / `app.manifest` version `0.1.1`; `templates_only_mode = true` baked into the staged conf and forced at every config-read path by the compiled flag — see the build-300 entry for the mechanism). On top of the build-328 state it carries the builds-329–332 arc described above (IP-endpoint name enrichment, the top-5 High Traffic cutoff, the decluttered donut legends, and the Data Doctor rollup-SPL copy/open-in-search actions). Its parity baseline (build 245) was produced by overlaying the v0.0.6 App source and restoring the six LLM provider modules (`AnthropicProvider` / `OpenAIProvider` / `AzureOpenAIProvider` / `BedrockProvider` + the SSE / Anthropic-event-translator helpers) plus the full provider factory, this line's `app.manifest`, and the `templates_only_mode = false` default; the bundle's vendor-endpoint fingerprint (`api.anthropic.com`, `api.openai.com`, `bedrock-runtime`, `anthropic-version`) matches the prior canonical full-LLM build. Builds 246–313 add the Magnetic re-theme, the topology star-system arc, dynamic model discovery, the blue-gradient icon set, the session-081 UX pass, the global Cloud Provider filter, the Linux/Windows dashboard-chart fixes, the AI-prompt rollup conversion + row-cap fixes, the configurable MCP request timeout (default 60s) + read-only server-timeout display, the sub-hour hybrid time-range rollout + global Refresh button + WebDispatcher cloud-filter fix, the build-299 after-hours window fix, the build-300 templates-only build gate, the build-301 stale-prompt-count sweep, the build-302/303 About dialog, the builds-306–313 **LogServ Data Doctor** (inline empty-panel hints, the diagnosis drawer + rollup source trace, the verdict-honesty pass, three-level PDF reports, the Diagnostics page + saved-report persistence, and the build-312 completion-path fix), the build-313 fresh-install verdict pass, and the build-314 **operator-supplied ingest evidence** (checks 27–29 — the Diagnostics page prints the command to read the Data TA’s ingest filters, accepts + credential-scrubs + parses the pasted output into the new `logserv_diag_ingest_facts` KV collection, and two provenance-badged cascade verdicts, `ingest-window-before-cutoff` and `ingest-type-excluded`, consume it) described above, and the builds-315/316 **Tier B platform snapshot + report completions** (design §16: a new hourly `logserv_diag_platform_aggregate` saved search copies scheduler outcomes/skips, search-concurrency warnings, per-index throughput, pipeline-queue depth and PCRE events from `index=_internal` into the new world-readable `logserv_diag_platform_snapshot` KV collection — the cron slot at `2 * * * *` was freed by moving the four ES anomaly dailies to `30 2..5 * * *`, behaviourally neutral; the Diagnostics page and the environment report gain a Platform-health section that renders NOT AVAILABLE with a verified reason whenever the snapshot is stale/empty/unreadable, and a stale rollup's verdict can carry a provenance-badged — never confidence-raising — scheduler-skip evidence line; the environment report gains the §7.6 Data-coverage section with an ASCII daily-volume chart, the clz-coverage denominator and host counts; the drawer gains the opt-in §7.10 raw-event samples — up to 5 events, credential-scrubbed + email/user-redacted, truncated to 500 chars, with a state-aware cover banner, and sample-bearing reports are download-only, never persisted; the drawer now closes on ANY route change — browser Back and in-app navigation — closing validation-plan C10; the U+2192 arrow that rendered as mojibake in every PDF was swept to ASCII across ~43 sites with a permanent cp1252 build gate; and the shipped-conf cron census is now a permanent build gate — 79 enabled scheduled searches, zero collisions), and the build-317 **Phase 5 deep evidence** (design §17/§17.8a — the drawer-only deep probes that complete the Data Doctor's check catalogue 1–29: check 21 reconciles a summarised panel against its byte-equal raw twin over the SETTLED window, producing the confirmed `cache-contradicted` when the raw arm returns rows the summary lacks and the health-certifying `cached-raw-agree-empty` when both agree — grouped raw arms only, since a scalar `\| stats count` twin carries no row-count signal; check 22 samples the panel's sourcetype for each filtered field's presence and expected values, producing `field-never-populated` (the missing-extraction shape) and `field-value-mismatch` (the vocabulary-drift shape); check 25 relaxes each base-search conjunct in turn with a control probe first, producing `clause-excludes-all` naming WHICH clause excludes every event; check 24 verifies non-rollup lookup registration (`lookup-not-registered`, dormant for the shipped panel set); check 17b adds a provider-scoped coverage evidence line to cached verdicts under a Cloud Provider filter; and check 18 adds a bucket-continuity "Recent buckets" column to the environment REPORT's rollup table — report-path only, never the live page. The deep probes run ONLY in the panel drawer (`deep: true`), never in the dashboard sweep. Build 317 also fixes a live defect the pre-implementation review caught: EnvironmentHealth's sub-hour hybrid raw arms still used the dead `audit_action` field — every HANA failed CONNECT was dropped from the Auth Failures KPI at sub-90-minute ranges and the criticalEvents Detail column was NULL for HANA rows; both now read `action_type`), and the build-318 **Phase 6 partial-data diagnosis** (design §18/§18.8a — the Data Doctor now covers panels that HAVE data: every chart/table toolbar gains a fifth **Diagnose this panel** icon and every KPI card a hover/focus-revealed corner affordance, opening the drawer under a “Panel diagnosis” header; the table renderers publish a VALUES-FREE column-coverage summary (counts only, through a bounded side channel — no row values ever reach the report/persistence layer), each fully-blank displayed column is resolved to its raw-event origin through the whole pipeline’s renames and corroborated against a sampled source-event dispatch, yielding `column-never-populated` (extraction gap) / `column-not-summarised` (summarisation gap) / `column-not-stored` (the aggregate’s keep-list provably lacks it — “a backfill cannot add it”), with the honest floor `panel-data-present` unreachable while any column is unprobed (`column-tier-incomplete` names them); zero-displaying KPIs route into a zero-value resolution — summarised rows or an agreeing scalar raw twin certify `panel-zero-confirmed` (never a backfill prescription), a disagreeing twin is the possible-grade `zero-value-mismatch`, never confirmed; a populated panel’s partial-mode gather SKIPS the emptiness battery with explicit “Not applicable — this panel returned data” ledger notes, and a still-loading panel is refused rather than diagnosed on unknown facts), and the build-319 **operator-supplied evidence refinements** (design §19/§19.8a — check 29 gains an INDEX-TIME contradiction comparator from `| metadata` recentTime that closes the replay/backdate evasion, capping the verdict at possible with a "recorded as indexed after the configuration was supplied" line and a 300-second clock-skew grace, never standing the gate down; check 28 gains pre-cutoff corroboration lines from the index-wide oldest event (firstTime, same dispatch, zero extra cost) with deliberately non-discriminating wording for the nothing-older case; the raw-tier zero-in-window verdicts gain a sliding-cutoff context line when the window ends before the supplied cutoff and the index is NOT empty; the Data TA’s Cloud Provider stamp is recovered from transforms/settings pastes — explicit stanzas only, never inferred from absence — stored in the new `cloud_provider_stamp` facts column and explained on provider-filtered panels including the aws-matches-unattributed nuance; the drawer + its Copy-technical-summary gain a pointer to the Diagnostics-page paste on ingest-ambiguous verdicts with no usable supply; plus the §19.8a-6 transforms-inverse correctness fix — the generic re.escape inverse so hyphen-bearing patterns recover faithfully — read-side clamp marking, and the H19 memoizing-runner fix that forwards per-probe runtime caps), and the build-320 **rollup SPL in reports + full-length raw samples** (design §20/§20.8a — the panel diagnosis drawer’s technical detail, its Copy-technical-summary, and the panel + dashboard PDFs now print the FULL SPL of the saved searches currently configured to populate the panel’s rollup collection(s): each entry is attributed by its PARSED `outputlookup` target (never the registry bundle — the 1:many beaconing/topology rollups made that distinction load-bearing), carries its cron verbatim + its last-modified timestamp, sits under a pinned "read at diagnosis time / NOT run by this diagnosis" intro, and names its install-backfill stanza with an as-shipped identity hedge whose premise a new build gate derives from the shipped conf; the dashboard PDF gets one de-duplicated section covering every rollup the dashboard reads, healthy panels included, with an explicit "N of M could not be read" completeness line; the SPL is stored exactly twice per model (section mono + `json.rollupSearches` — gate-pinned) and the sweep’s evidence never carries it. Raw event samples are no longer truncated to 500 characters: each opt-in sample is included in full up to a disclosed 20,000-character safety ceiling applied BEFORE redaction, the e-mail redactor’s quadratic local-part match is bounded (linear on any input), the credential scrubber gains space-delimited SQL/CLI secret shapes (`PASSWORD "x"` / `IDENTIFIED BY x` / `-P x` / `-pXxx` / `--password x`), the samples paragraph + cover banner add a Latin-1-font fidelity clause pointing at the byte-exact .json twin, the banner’s inventory names the new saved-search-body content class, and `LEGACY_DATA_BANNERS` keeps every report stored by earlier builds re-downloadable), and the builds-321–328 **Environment Topology arc** described above (the build-321 Edge Details fix, the builds-322/323 node-panel clarity pass + uncapped legends and wedge tooltips, the build-324 High/Regular-Traffic classification + node icons + tenant circles + dividers, the build-325 host/instance visibility + RFC per-app-server key fix + "By app server" table, and the builds-326–328 group select + group move with the tenant-chip and Escape-guard fixes) — the tarball grew ~0.7 MB over build 245 from the bundled font files (Inter 400/500/600/700 + Sharp Sans Bold + Roboto Mono 400 under `appserver/static/fonts/`, attributed in `THIRD-PARTY-NOTICES.md` + `LICENSES/`). Live-verified end-to-end on the test fleet at builds 272–277: dashboards + KV-Store rollups render in both light and dark mode, the topology star-system layout + floating edges verified in both modes (node/edge selection, saved layouts, Live-Activity re-fit, the build-277 secondary-SID sizing user-verified visually), the topology PNG export pixel-verified (12/12 sampled edge midpoints + 10/10 sampled health-ring points render in the exported image), free-form chat dispatches through the provider factory + outbound guard, the Provider Credentials Settings tab is visible with the Templates-only toggle off, and model discovery verified against the live Anthropic `/v1/models` endpoint (10-model merged picker, TTL / manual / failure paths + audit events all exercised).

The **Data TA is at internal `app.conf` version `0.1.1`** (md5 `b87e00de84d89c47509e7d820ff53278` — the 2026-08-18 canonical rebuild; the prior blue-icon canonical, md5 `8b5ecaa86a26ce4be473164fc8d8b199`, is preserved in [`../testing/iteration_tarballs/`](../testing/iteration_tarballs/) as `splunk_ta_sap_logserv-0.1.1-blueicon-8b5ecaa8.tar.gz`, and the earlier gcp-dropdown build, md5 `14f31186ee66302b0d84cde08a116dd8`, as `splunk_ta_sap_logserv-0.1.1-gcpdd.tar.gz`). Functionally identical to the blue-icon respin (see the 2026-08-18 promotion note above for the exact member-diff), it adds **"Google Cloud Platform" (`gcp`) as a fourth Configuration → Cloud Provider dropdown choice** (Not set / AWS / Microsoft Azure / Google Cloud Platform) on top of the prior filesystem-fallback ("fsfix") + hook-fix build. It carries no ingest input; data collection is the forwarder-tier add-ons' job (`Splunk_TA_aws` for AWS S3, the Azure and GCP add-ons below for their clouds), each emitting `sourcetype = sap_logserv_logs` into this TA's shared index-time pipeline.

The **Azure and GCP ingest add-ons now version with the release line at `0.1.1`** (rebuilt 2026-08-18; content otherwise identical to the 0.0.6 blue-icon builds apart from the version stamps and the `packaging` library refresh — the outgoing 0.0.6 canonicals are preserved in `../testing/iteration_tarballs/` as `…_azure-0.0.6-blueicon-6ce79884.tar.gz` / `…_gcp-0.0.6-blueicon-9c5876b3.tar.gz`, alongside the earlier `…-pre-blueicon.tar.gz` builds; the frozen v0.0.6 line's release binaries keep shipping their own 0.0.6-versioned copies): standalone Heavy-Forwarder-tier TAs providing notification-driven ingest — `sap_logserv_azure_queue` (Event Grid → Storage Queue → SAS blob fetch) and `sap_logserv_gcp_pubsub` (GCS OBJECT_FINALIZE → Pub/Sub pull → service-account object fetch + gunzip). Both are stdlib-only and install **directly on each Heavy Forwarder, never via the Deployment Server** (their credentials live in each add-on's own `local/passwords.conf`). See the Azure Setup and GCP Setup pages in `../docs/content/install-setup/`.

All four are installable via Splunk Web (**Apps → Install app from file**) or via CLI:
```bash
/opt/splunk/bin/splunk install app /path/to/<tarball>
```
(Install the App on the Search Head, the Data TA on the Deployment Server + Heavy Forwarders + Indexer, and — only for Azure / GCP ingest — the matching cloud add-on **directly on each Heavy Forwarder, not as a deployment-app**.)

## The AI Assistant in this build

**The canonical tarball here is the templates-only variant**: the AI Assistant's predefined-prompt path (61 curated saved-search prompts, dispatched via the Splunk MCP Server, Splunkbase 7931) is fully functional and needs **no** vendor credential; the free-form LLM path is **disabled at compile time** — the chat input is read-only with an explanatory banner, and the Provider Credentials tab, model pickers, model discovery and Power Mode are hidden/inert. No runtime setting can re-enable it in this build. On a fresh install at high event volume, run **Settings → Dashboard Data → Run backfill** once to seed the dashboard rollup collections (same as v0.0.6).

For a deployment that should have the free-form LLM path, use the archived **full-LLM variant of the same build** (`../testing/iteration_tarballs/splunk_app_sap_logserv-0.1.1-build333.tar.gz`) or build it from this source with plain `yarn build`. In that variant: one provider credential under **Settings → AI Assistant → Provider Credentials** (encrypted password store; realm convention `logserv_ai_assistant_<provider>`) enables the LLM path; privacy tiers govern what any vendor sees (**no raw Splunk event data is ever transmitted — type-system-enforced**); the runtime `templates_only_mode` toggle can restrict it back to canned prompts with no rebuild; and the model pickers refresh from the configured vendor's model-listing endpoint (the **Model discovery** Settings toggle + "Refresh model list" button + `model_discovery` audit trail).

## AppInspect (Cloud-mode precert) posture

All runs use `--mode precert --included-tags cloud` (AppInspect 4.1.3):

- **App** (build 334, **templates-only variant**, run 2026-08-19): 0 errors / 0 failures / 0 future_failures / 8 baseline warnings / 109 success — see [`appinspect_cloud_logserv_app.json`](./appinspect_cloud_logserv_app.json). The full-LLM variant of the same build was separately run the same day with the identical posture. Identical gating posture to build 245 and the v0.0.6 App; the re-theme (including the bundled fonts), the topology arc, the export fix, model discovery (the new `logserv_ai_models` collection), the secondary-SID resize, the blue-gradient icon set + DocsHelpIcon conversion, the session-081 UX pass, the Cloud Provider filter (the `cloud_provider` rollup-grain extension), the build-283/284 Linux/Windows dashboard-chart fixes, the build-285/286 AI-prompt rollup conversion + row-cap fixes (new metric arms + collection fields, conf-only + webapp-bundled intent map), the build-287/288/289 configurable MCP timeout (default 60s) + read-only server-timeout display (one new settings field + one cross-app read, webapp + conf), the build-290–298 sub-hour hybrid time-range rollout + global Refresh button + WebDispatcher cloud-filter fix (webapp-only — no conf/manifest/rollup change), the build-299 after-hours window fix (webapp + props.conf/savedsearches.conf/intent-map edits, no new conf files or collections), the build-300 templates-only build gate (webapp-only, plus a build-script patch that only runs in the templates-only variant — the canonical full-LLM tarball's conf is unchanged), the build-301 stale-prompt-count sweep (one webapp string + one conf comment), the build-302/303 About dialog (one new webapp component + a compile-time app.conf read + build-date stamp + the app icon copied into `appserver/static/`), and the builds-306–313 LogServ Data Doctor (webapp + one new KV Store collection `logserv_diag_reports` with its `collections.conf` / `transforms.conf` / `default.meta` stanzas + one scheduled retention search at the collision-free `56 1 * * *` slot, plus the build-306 SPL-defect fixes in `savedsearches.conf` and the intent map; build 312 is a webapp-only fix on top), and the build-314 operator-supplied ingest evidence (webapp + one new KV Store collection `logserv_diag_ingest_facts` with its `collections.conf` + `default.meta` stanzas — deliberately no `transforms.conf` stanza and no scheduled search), and the builds-315/316 Tier B platform snapshot (webapp + one new KV Store collection `logserv_diag_platform_snapshot` with its full `collections.conf` / `transforms.conf` / `default.meta` trio + two scheduled searches — the hourly aggregate at `2 * * * *` and the daily retention at `58 1 * * *` — plus the four ES anomaly cron moves to `30 2..5 * * *`), and the build-317 Phase 5 deep evidence (webapp-only — no new conf files, collections or scheduled searches; the raw-twin side channel, the deep-probe gates and the EnvironmentHealth `audit_action` → `action_type` fix all live in the bundle), and the build-318 Phase 6 partial-data diagnosis (webapp-only — no new conf files, collections or scheduled searches; the column-coverage side channel, the partial-mode gates, the Diagnose toolbar icon and the KPI corner affordance all live in the bundle), and the build-319 evidence refinements (webapp + ONE new field on the existing `logserv_diag_ingest_facts` collection — `field.cloud_provider_stamp` in `collections.conf`; no new collections, scheduled searches or transforms stanzas), and the build-320 rollup-SPL surfacing + full-length samples (webapp-only — no new conf files, collections or scheduled searches; the producer-SPL capture, the report sections, the sample-ceiling change and the scrubber/redactor hardening all live in the bundle), and the build-321 Edge Details fix (webapp-only — no new conf files, collections or scheduled searches; the edge-identity module, the rollup reads and the corrected labels all live in the bundle), and the build-322 node-panel clarity pass (webapp-only — no new conf files, collections or scheduled searches; the panel-facts module, the two donuts, the ownership badges and the per-host traffic table all live in the bundle, and the one SPL change is an added `eventstats` inside an existing rollup read), and the build-323 legend + wedge-tooltip follow-up (webapp-only), and the build-324 High/Regular-Traffic + node-icons pass (webapp-only), and the build-325 host/instance visibility + RFC per-app-server key fix (webapp + `savedsearches.conf` edits — the RFC `_key` case-branch in the topology edges aggregate/backfill, the `instances` measure in the detail aggregate/backfill, the seed removal — plus the `local_ip` / `instances` field additions to the existing topology collections' `collections.conf` / `transforms.conf` stanzas; no new conf files, collections or scheduled searches), and the builds-326–328 group select + move (webapp-only — interaction props, selection state, node-chrome outlines, the hint chip, the tenant-chip margin and the Escape visible-dialog guard all live in the bundle), and the builds-329–332 arc (the `logserv_topology_ip_enrichment` collection with its full conf trio + daily aggregate/backfill/retention searches at collision-free slots, plus the webapp-only builds 331/332), and the build-333 elkjs-attribution respin (a `package.json`/`yarn.lock` dependency declaration + the regenerated `THIRD-PARTY-NOTICES.md` — no bundle, conf or collection change), and the build-334 string-accuracy pass (webapp strings + `savedsearches.conf` / `ai_assistant_settings.conf` comment-and-description edits + one `collections.conf` field-type line — no logic, collection, or scheduled-search change) add no findings. The watched `check_for_existence_of_python_code_block_in_mako_template` check remains a **success** (not a warning).
- **Data TA** (0.1.1, 2026-08-18 canonical rebuild): 0 errors / 0 failures / 0 future_failures / 11 baseline warnings / 123 success — see [`appinspect_cloud_logserv_ta.json`](./appinspect_cloud_logserv_ta.json).
- **Azure TA** (0.1.1, 2026-08-18 rebuild): 0 / 0 / 0 / 10 / 111 — see [`appinspect_cloud_logserv_azure_ta.json`](./appinspect_cloud_logserv_azure_ta.json).
- **GCP TA** (0.1.1, 2026-08-18 rebuild): 0 / 0 / 0 / 10 / 111 — see [`appinspect_cloud_logserv_gcp_ta.json`](./appinspect_cloud_logserv_gcp_ta.json).

## Prior canonical builds + history

Preserved under [`../testing/iteration_tarballs/`](../testing/iteration_tarballs/): the prior canonical App builds 245 (`…-build245.tar.gz`, md5 `43c7b32f80eeebf38ebefffd025205cd` — the parity baseline, pre-re-theme), 272 (`…-build272.tar.gz`, md5 `c2374c23f71c3b8d43236764a6121d4e` — the Phase-5 promotion, before the export fix) and 274 (`…-build274.tar.gz`, md5 `56a55cf6b35d3209cd54ac8d70f0713b` — the export-fix promotion, before model discovery); the interim builds 271 (`866c3888de87f5b23c2ca2def7e716de` — the light-mode contrast sweep before the re-fit hardening), 273 (`474636897caab2db180f73250a073f76` — the export fix before the ring-transform correction), 275 (`…-build275.tar.gz` — model discovery Phase 1: Anthropic / OpenAI / Mock) and 276 (`…-build276.tar.gz`, md5 `0c1f504b0836328fe58e345b6035aeac` — model discovery Phase 2: Azure + Bedrock, before the secondary-SID resize); the prior canonical 277 (`…-build277.tar.gz`, md5 `544114e3e68b9fe1cff7d242852eb65c` — the model-discovery + secondary-SID-resize promotion, before the blue-icon branding); the prior canonical 278 (`…-build278.tar.gz`, md5 `909d6cbaa148e64b2708cf6d65d01567` — the blue-icon promotion, before the session-081 UX pass); the interim builds 279 (`…-build279.tar.gz` — the Settings readability pass + a viewport-measured topology page-sizing attempt that regressed into page scrolling, superseded same-session) and 280 (`…-build280.tar.gz`, md5 `9a9cc3893cc16d92cc0ea12ebf046597` — the page-sizing revert + Live Activity panel fix, before the link-graph restyle); the prior canonical 281 (`…-build281.tar.gz`, md5 `bc698d47b9f17bc3ebbb68a412fa119b` — the session-081 UX-pass promotion, before the Cloud Provider filter); the prior canonical 282 (`…-build282.tar.gz`, md5 `82c1c393ea3b20c1adfa645c8a6c1ede` — the Cloud Provider filter promotion, before the Linux/Windows chart fixes); the interim build 283 (`…-build283.tar.gz`, md5 `56b343e2014f68cb8b2dea4e8c40ad39` — the Linux horizontal-bar fix, before the Windows chart fixes); the prior canonical 284 (`…-build284.tar.gz`, md5 `4f2b229aa04c053487cdb316d41f8ee0` — the Linux/Windows chart-fix promotion, before the AI-prompt rollup conversion); the prior canonical 285 (`…-build285.tar.gz`, md5 `04eb3ab09ba4991c41f1c75636d5f8fd` — the rollup-conversion promotion, before the row-cap + ES-prompt fixes); the prior canonical 286 (`…-build286.tar.gz`, md5 `389f0281398adb10d89d5abb30985057` — the row-cap + ES-prompt-fix promotion, before the configurable MCP timeout); the prior canonical 287 (`…-build287.tar.gz`, md5 `fd9209c9a1a6c6be742c1ad75c905daa` — the configurable-client-timeout promotion, before the read-only server-timeout display); the prior canonical 288 (`…-build288.tar.gz`, md5 `7326dcb768f00d6f2479f8556fe7fb69` — the read-only server-timeout-display promotion, before the 60s default); the prior canonical 289 (`…-build289.tar.gz`, md5 `53da8bffd4a1417dddf311933b850fcf` — the 60s-default promotion, before the sub-hour hybrid rollout); the interim builds 290–297 (`…-build290.tar.gz` through `…-build297.tar.gz` — the incremental sub-hour hybrid dashboard rollout + the global Refresh button, superseded by the WebDispatcher cloud-filter fix); the prior canonical 298 (`…-build298.tar.gz`, md5 `54b0e0a1146c20da45bf0912bc0fa2e6` — the sub-hour-hybrid + Refresh-button + cloud-filter promotion, before the after-hours window fix); the prior canonical 299 (`…-build299.tar.gz`, md5 `87b2e5126a15ffe0203223dfd24a22e9` — the after-hours-window-fix promotion, before the templates-only build gate); the prior canonical 300 (`…-build300.tar.gz`, md5 `96da6f9090ad86823ee915f5e88f23f1` — the templates-only-build-gate promotion, before the stale-prompt-count sweep); the prior canonical 301 (`…-build301.tar.gz`, md5 `8857ecf647174fdc4586b315e8fc6cbe` — the stale-prompt-count promotion, before the About dialog); the prior canonical 302 (`…-build302.tar.gz`, md5 `4f008235569e23988cbd31d00463543d` — the About dialog before the build date was added); the prior canonical 303 (`…-build303.tar.gz`, md5 `53552a3fd49770764f7230a9a752e836` — the About-dialog promotion, before the Data Doctor arc); the interim Data Doctor builds 304 (`e6f25db2299f5c3309bf98fea89f27fa` — the Phase-1 inline-hints build; its archived tarball is incomplete, missing the third-party notices + SBOM — the pipe-through-`head` incident, superseded same-day), 305 (`32888d9b0721ee2ab28ca49dc41260c2` — the hint-attribution fix), 306 (`43cd56a47abd5387031aad30d94f9dfa` — the free-check hints + KPI-card compact hints + the four live SPL-defect fixes), 307 (`28cb85b1113a2807d3399e147950f9b9` — the diagnosis drawer + evidence cascade), 308 (`b0fe797516289cf0453aa564ca2ea4e0` — the rollup source trace), 309 (`83ce02adbb1e1f821bdde4934c1979ed` — the verdict-honesty pass) and 310 (`d4566607e4f4b7d100413201b86074fa` — the three-level PDF reports); the prior canonical 311 (`…-build311.tar.gz`, md5 `9bbcd843edccfedc0f58ff19838f4148` — the initial Data Doctor promotion, superseded the same day by the build-312 completion-path fix after the rendered-UI pass caught the Diagnostics page discarding every completed run as a false supersede); the prior canonical 312 (`…-build312.tar.gz`, md5 `39911a29848550b54891321c8e1f82ac` — the completion-path-fix promotion, before the fresh-install verdict pass); the prior canonical 313 (`…-build313.tar.gz`, md5 `30974407bf0cfa08b77649af34b5cc99` — the fresh-install verdict-pass promotion, before the operator-supplied ingest evidence); the prior canonical 314 (`…-build314.tar.gz`, md5 `a44640e84014f3d0a9d0c8d2cc12967c` — the operator-supplied-ingest-evidence promotion, before the Tier B platform snapshot); the interim build 315 (`…-build315.tar.gz`, md5 `e6cc4d5450fddb71bb448270694cbdf8` — the Tier B snapshot before the daily-series ISO-timestamp parse fix the rendered pass caught); the prior canonical 316 (`…-build316.tar.gz`, md5 `ad49f2c5cdf1b98fe93d6c86443df854` — the Tier-B-snapshot promotion, before the Phase-5 deep-evidence pass); the prior canonical 317 (`…-build317.tar.gz`, md5 `e642f608ead62c91418839345082ad58` — the Phase-5 deep-evidence promotion, before the Phase-6 partial-data diagnosis); the prior canonical 318 (`…-build318.tar.gz`, md5 `f0d8f299e1320d057a1466fe9c481513` — the Phase-6 promotion, before the §19 evidence refinements); the prior canonical 319 (`…-build319.tar.gz`, md5 `c86c0f188975193bd051b6bb566a9acc` — the §19-refinements promotion, before the §20 rollup-SPL + full-length-samples enrichments); the prior canonical 320 (`…-build320.tar.gz`, md5 `8140ddd37d35a90dd0c20ef86c4999a3` — the §20 rollup-SPL + full-length-samples promotion, before the Edge Details fix); the prior canonical 321 (`…-build321.tar.gz`, md5 `01a9f3b3ccdcf5c049414ead07f17ad6` — the Edge Details fix, before the node-panel clarity pass); the prior canonical 322 (`…-build322.tar.gz`, md5 `c25e407b46bc7730ddc4eb18b4a478ac` — the node-panel clarity pass, before the uncapped legend + wedge tooltips); the prior canonical 323 (`…-build323.tar.gz`, md5 `6fad13a9dc955006066c7bb64f12c0a7` — the uncapped-legend + wedge-tooltip promotion, before the High/Regular-Traffic + host-visibility arc); the interim build 324 (`…-build324.tar.gz`, md5 `05cde6d4045268f5295d81d57cec6923` — the High/Regular-Traffic classification + node icons + tenant circles + dividers, before the host/instance visibility + RFC per-app-server key fix); the prior canonical 325 (`…-build325.tar.gz`, md5 `f649370594e01a605c6de21c1348823b` — the host/instance-visibility + RFC-key promotion, before the group-select arc); the interim builds 326 (`…-build326.tar.gz`, md5 `2c42bf67e6d8483e3bd03d0bc445e9d2` — group select + move, before the tenant-chip fix) and 327 (`…-build327.tar.gz`, md5 `6f072104ecc78ea5733ed83c7db0b446` — the tenant-chip clearance fix, before the Escape visible-dialog guard); the prior canonical 328 (`…-build328.tar.gz`, md5 `572facb956be50fa05fbeb64ea54b24f` — the group-select promotion, before the IP-enrichment arc); the interim builds 329 (`…-build329.tar.gz`, md5 `fb8c707d5253bd45012214ddca69bfab` — the IP-enrichment build superseded pre-deploy by the saved-search-prefix fix), 330 (`…-build330.tar.gz`, md5 `88381560d24463f24a51824807242980` — IP-endpoint name enrichment) and 331 (`…-build331.tar.gz`, md5 `86399a3e8b6b337d58394045b1d64138` — the top-5 High Traffic cutoff + legend declutter, never promoted); both variants of the prior canonical build 332 — full-LLM (`…-build332.tar.gz`, md5 `9b1f087a7c0c50d672245fe1c7dd9154`) and templates-only (`…-build332-templates-only.tar.gz`, md5 `bdd222ba7b3bdd38ef789d889f7779d7`) — superseded by the build-333 elkjs-attribution respin; both variants of the prior canonical build 333 — full-LLM (`…-build333.tar.gz`, md5 `2203f6cfca9658872edea7b1821d6167`) and templates-only (`…-build333-templates-only.tar.gz`, md5 `63e91f40d5a41a9713cf07f465e673e5`) — superseded by the build-334 string-accuracy pass; **both variants of the current build 334** — the **full-LLM variant** (`…-build334.tar.gz`, md5 `043e84610e7434fd69b92525ccb6e69f`, deployed on the test fleet) and a build-suffixed copy of the canonical **templates-only variant** (`…-build334-templates-only.tar.gz`, md5 `3f9c77583ee8de6f201a8a327cc38420`); build 300's templates-only variant (`…-build300-templates-only.tar.gz`, md5 `650cd4e62564c22bbec17e0aa1271953`); the outgoing 2026-07-07 TA canonicals — the blue-icon Data TA (`splunk_ta_sap_logserv-0.1.1-blueicon-8b5ecaa8.tar.gz`, md5 `8b5ecaa86a26ce4be473164fc8d8b199`) and the blue-icon 0.0.6 Azure/GCP add-ons (`…_azure-0.0.6-blueicon-6ce79884.tar.gz` md5 `6ce7988464d59a76c03b945ece3c62e5`, `…_gcp-0.0.6-blueicon-9c5876b3.tar.gz` md5 `9c5876b3588bec5c4732de13d3fef64e`) — plus build-suffixed copies of the new 0.1.1 TA builds (`splunk_ta_sap_logserv-0.1.1-rebuild-b87e00de.tar.gz`, `…_azure-0.1.1-ffb8bb15.tar.gz`, `…_gcp-0.1.1-93306c0c.tar.gz`); the pre-blue-icon Azure/GCP add-on builds (`…_azure-0.0.6-pre-blueicon.tar.gz` md5 `6eae44d0df06489127fa9815df55485f`, `…_gcp-0.0.6-pre-blueicon.tar.gz` md5 `21a6db54b97878af9ed1489b5d76ee7a`); the pre-parity App build 244 (`ad40f726bba6bf4931695a934f98b8db` — the 2026-06 App, WITHOUT the dashboard-performance data layer); and the pre-gcp-dropdown Data TA (`splunk_ta_sap_logserv-0.1.1-fsfix.tar.gz`, md5 `94fdf73d4fdb423090add3d75fe7e91a`). The detailed build-236→244 change history (ISC BIND/Squid absorption back-port, sc_subadmin enablement, KV-Store settings migration, audit-index rename, the session-043 Data TA metadata patcher, etc.) lives in the preserved prior README at `../testing/iteration_tarballs/README-build244-era.md`. The pre-overlay App + docs sources are preserved at `../testing/pre_v006_sync_app_backup_2026-07-05/` and `../testing/pre_v006_sync_docs_backup_2026-07-05/`.

## Upgrade note (from App build 244 or earlier)

Upgrading a v0.1.1 deployment to build 245 moves the dashboards onto the KV-Store rollup data layer: dashboards read hourly-aggregated collections and look empty until the one-click **Settings → Dashboard Data → Run backfill** runs (or the hourly aggregation seeds them over the following day). The Enterprise Security content ships **enabled** on a collision-free staggered schedule. See `../docs/content/getting-started/upgrading.md`.

**Upgrading a full-LLM v0.1.1 install with THIS canonical tarball disables free-form chat**: the templates-only variant forces `templates_only_mode` on at every config-read path, so a stored KV setting of `0` (the state any previously-LLM-enabled install has) does not re-enable it. Stored provider credentials are untouched and become active again if the full-LLM variant is installed later. Install the archived full-LLM variant instead if that is not the intent.

## Splunkbase submission status

**Held.** As of 2026-08-18 the canonical v0.1.1 App artifact is the **templates-only build variant** (LLM path compile-time disabled); the full-LLM variant of the same build remains archived for internal / approved distribution. The frozen v0.0.6 line remains the LLM-STRIPPED public-publishable alternative. Submission decisions are handled by the maintainer who holds Splunkbase credentials.
